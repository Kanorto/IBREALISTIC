# План реалистичного ралли — IBRealistic

> **Архитектура:** IBRealistic — аддон к OpenBoatUtils (OBU). OBU используется как неизменяемая зависимость (пакеты 0–32 на `openboatutils:settings`). IBRealistic — основной проект, обрабатывает пакеты 33–69 + 80–88 на `ibrealistic:settings`. Пакет: `dev.kanorto.ibrealistic`. VERSION протокола: **21** (→ **23** после Фазы 17-18).
>
> **⚠️ OBU (OpenBoatUtils-main/) — НЕИЗМЕНЯЕМ.** Это внешняя зависимость, код OBU нельзя модифицировать. Все изменения делаются ТОЛЬКО в IBRealistic/ и TimingSystem/.
>
> **Структура документа:** Завершённые фазы (1–5b) → Известные баги → Текущие/будущие фазы (6–20) с порядком зависимостей.
> Каждый шаг включает работу с переводами (lang/*.yml + triton.yml + triton/timingsystem.json).
> Функционал всегда реализуется **до** визуала: нельзя делать GUI без готового бэкенда.

---

## Порядок зависимостей между фазами

```
ФАЗА 6  (Баги и стабильность)      ← ✅ завершено
   ↓
ФАЗА 7  (Версионирование серверов) ← ✅ завершено
   ↓
ФАЗА 8  (Экономика: валюта + XP)   ← ✅ завершено
   ↓
ФАЗА 9  (Кастомизация автомобилей) ← ✅ завершено
   ↓
ФАЗА 10 (Система гонок)            ← ✅ завершено
   ↓
ФАЗА 11 (Настройки треков)         ← ✅ завершено
   ↓
ФАЗА 12 (Раллийные процедуры)      ← ✅ завершено
   ↓
ФАЗА 13 (Система поломок и износа) ← ✅ завершено
   ↓
ФАЗА 14 (Античит)                  ← ✅ завершено
   ↓
ФАЗА 15 (Визуал, GUI и полировка)  ← ✅ 15.1-15.8 завершены, 15.9 = backlog
   ↓
─── НОВЫЕ ФАЗЫ (высокий приоритет) ───
   ↓
ФАЗА 17 (Телеметрия и валидация)   ← основа для ghost, аналитики и античита
   ↓
ФАЗА 18 (Ghost Racing / Replay)    ← зависит от фазы 17 (телеметрия)
   ↓
ФАЗА 19 (Автоматические турниры)   ← независимая, но после базового функционала
   ↓
ФАЗА 20 (Командные гонки + пит)    ← зависит от фаз 13 (ремонт) + 10 (мультиплеер)
   ↓
ФАЗА 16 (Бэклог)                   ← идеи для дальнейшего развития
```

---

# ЗАВЕРШЁННЫЕ ФАЗЫ

## ФАЗА 1: ФИЗИКА ДВИЖЕНИЯ (Мод) ✅

### 1.1 Bicycle Model (Велосипедная модель) ✅
- [x] Состояния: vx, vy, yawAngle, yawRate, steeringAngle
- [x] Ускорения предыдущего тика для массообмена: axPrev, ayPrev
- [x] Нагрузка на оси: fzFront, fzRear
- [x] Релаксация боковых сил: fyFrontActual, fyRearActual
- **Файл:** `physics/RealisticPhysicsEngine.java`

### 1.2 Типы машин (VehicleType) ✅
- [x] WRC_CAR: 1190кг, база 2.53м, ЦТ 0.45м, развесовка 55/45, **AWD**
- [x] GROUP_B: 1100кг, база 2.40м, ЦТ 0.50м, развесовка 45/55, **RWD**
- [x] CLASSIC_RALLY: 1000кг, база 2.45м, ЦТ 0.55м, развесовка 50/50, **RWD**
- [x] LIGHTWEIGHT: 800кг, база 2.30м, ЦТ 0.42м, развесовка 60/40, **FWD**
- [x] TRUCK: 2000кг, база 3.20м, ЦТ 0.90м, развесовка 50/50, **AWD**
- **Файл:** `physics/VehicleType.java`

### 1.3 Конфигурация машины (VehicleConfig) ✅
- [x] mass, wheelbase, cgHeight, trackWidth
- [x] frontWeightBias, maxSteeringAngle, steeringSpeed
- [x] brakingForce, engineForce, dragCoefficient
- [x] rollingResistance, brakeBias, engineBraking
- [x] substeps, speedSteeringFactor, rollStiffnessRatioFront
- [x] **drivetrain** (FWD / RWD / AWD)
- [x] Расчёт статических нагрузок осей
- **Файл:** `physics/VehicleConfig.java`

### 1.3a Тип привода (DrivetrainType) ✅
- [x] RWD (задний, 0% перед / 100% зад), FWD (передний, 100/0), AWD (полный, 50/50)
- [x] getFrontDriveRatio() определяет распределение тяги между осями
- **Файл:** `physics/DrivetrainType.java`

### 1.4 Массообмен (Weight Transfer) ✅
- [x] Продольный: ΔFz = (m × ax × h) / L
- [x] Поперечный: ΔFz = (m × ay × h) / T
- [x] Распределение по осям через rollStiffnessRatio
- [x] Защита от отрицательных нагрузок (clamp >= 0)

### 1.5 Tire Model (Fiala/Brush) ✅
- [x] Slip angle, нелинейная боковая сила, Friction Circle, Load Sensitivity
- **Файл:** `physics/TireModel.java`

### 1.6 Relaxation Length ✅
- [x] Фильтр первого порядка с разными σ для поверхностей

### 1.7 Руль (Steering) ✅
- [x] Ограничение скорости поворота, Speed-dependent ratio, задержка реакции

### 1.8 Поверхности (Surface System) ✅
- [x] 17 пресетов + BLUE_ICE
- [x] 300+ блоков Minecraft привязаны к поверхностям
- [x] Настраиваемая поверхность по умолчанию (defaultSurface)
- **Файл:** `physics/SurfaceProperties.java`

### 1.9 Вертикальная физика ✅
- [x] Детекция полёта, приземление, потеря/восстановление сцепления
- [x] Визуальный pitch в полёте, фильтрация мелких кочек

### 1.10 Sub-stepping ✅
- [x] 4-10 подшагов на тик (80–200 Hz)

### 1.11 Торможение ✅
- [x] BrakeBias, ручной тормоз, блокировка колёс через friction circle

### 1.12 Аэродинамика ✅
- [x] Drag, Rolling resistance, Engine braking

### 1.13 Интеграция в мод ✅
- [x] BoatMixin, Modes, ClientboundPackets, SingleplayerCommands
- [x] VERSION: 18 → 19 → 20 → **21** (текущий)
- [x] Канал: `ibrealistic:settings` (пакеты 33–69), OBU-пакеты (0–32) делегированы OBU
- **Пакет:** `dev.kanorto.ibrealistic`

## ФАЗА 2: СЕРВЕРНАЯ СТОРОНА (Плагин) ✅

### 2.1 BoatUtilsMode ✅
- [x] 7 реалистичных режимов (REALISTIC, REALISTIC_WRC, REALISTIC_GROUP_B, REALISTIC_CLASSIC, REALISTIC_LIGHTWEIGHT, REALISTIC_TRUCK, REALISTIC_ALLTERRAIN)

### 2.2 CustomBoatUtilsMode ✅
- [x] 15+ полей @Expose, 15+ packet ID (33–60), helperы отправки пакетов

### 2.3 CommandBoatUtilsModeEdit ✅
- [x] 13 свойств в `set`, подкоманды `addsurfacetype`, `clearsurfacetypes`, `info`

## ФАЗА 3: ВИЗУАЛЬНАЯ ФИЗИКА ✅

### 3.1 Визуальный наклон (Pitch, Roll) ✅
- [x] Pitch из продольного ускорения, Roll из бокового, Steering angle в PhysicsResult

### 3.2 Визуальные колёса ✅
- [x] 4 колеса с вращением, передние поворачиваются по рулю
- [x] Рулевое колесо с 45° наклоном колонки
- [x] Визуальный lift лодки (VISUAL_LIFT=0.25f)
- **Файлы:** `client/WheelRenderer.java`, `client/SteeringWheelRenderer.java`

### 3.3 Возврат руля (Self-Aligning Torque) ✅
- [x] steeringReturnRate, speed-dependent, настраиваемый

## ФАЗА 3a: ЧЕТЫРЁХКОЛЁСНАЯ СИСТЕМА И ПОГОДА ✅

### 3a.1 FourWheelPhysicsEngine ✅
- [x] Per-wheel slip angles, forces, нагрузки
- [x] Дифференциалы: Open, Locked, LSD
- [x] AWD split настраиваемый (awdFrontSplit)
- **Файл:** `physics/FourWheelPhysicsEngine.java`

### 3a.2 WeatherCondition ✅
- [x] CLEAR, RAIN, HEAVY_RAIN, SNOW, FOG — глобальный модификатор μ
- **Файл:** `physics/WeatherCondition.java`

### 3a.3 Downforce ✅
- [x] Аэродинамический прижим с распределением перед/зад

### 3a.4 Новые пакеты (VERSION 20→21) ✅
- [x] SET_AWD_FRONT_SPLIT (53), SET_FRONT/REAR_DIFFERENTIAL (54/55), SET_LSD_LOCKING_COEFF (56)
- [x] SET_DOWNFORCE_COEFFICIENT (57), SET_DOWNFORCE_FRONT_BIAS (58), SET_WEATHER_CONDITION (59)
- [x] SET_STEERING_RETURN_RATE (60), REALISTIC_SERVER_INFO (61), пресеты (62–68), SET_RACE_COUNTDOWN (69)

## ФАЗА 4: CI/CD ✅

- [x] build-release.yml: автосборка по тегу `vX.X.X` (Gradle + Maven + Java 21)
- [x] prerelease.yml: пререлизы при merge PR (формат `x.x.x.N`)
- [x] Загрузка JAR-файлов в GitHub Release

## ФАЗА 5: СИСТЕМА СПАВНА ИГРОКОВ ✅

- [x] Телепортация на спавн (join + respawn)
- [x] Хотбар-предметы (NETHER_STAR→/tt, RECOVERY_COMPASS→/reset, CHERRY_BOAT→/b)
- [x] Кулдауны, запрет выброса, Triton-переводы

## ФАЗА 5b: РЕФАКТОРИНГ В АДДОН OBU ✅
> **Суть:** IBRealistic переведён из самостоятельного мода в аддон к OpenBoatUtils.
> OBU теперь обрабатывает базовые пакеты (0–32), IBRealistic — только реалистичные (33–69).

- [x] Зависимость `openboatutils >= 0.4.10` в `fabric.mod.json` и `build.gradle` (`modCompileOnly`)
- [x] Удалены ~25 дублированных OBU полей и ~30 методов из `IBRealistic.java`
- [x] `ClientboundPackets.java` обрабатывает только пакеты 33–69 (+ case 0 RESET для IBRealistic-состояния)
- [x] Двухканальная маршрутизация в `SingleplayerCommands.java`: OBU-пакеты через `OpenBoatUtils.sendPacketS2C()`, IBRealistic — через `IBRealistic.sendPacketS2C()`
- [x] `Modes.java` делегирует OBU-настройки в `OpenBoatUtils.setXxx()`
- [x] Удалены `CollisionMode.java` и `ISettingContext.java` (используются OBU-версии)
- [x] BoatMixin: 5 уникальных хуков (liftPassenger, realisticPhysicsTick, cancelVanillaPaddles, cancelVanillaVelocityDecay, moveHook)
- [x] CI/CD обновлён: OBU собирается перед IBRealistic, JAR копируется в `libs/`
- [x] Пакет: `dev.kanorto.ibrealistic`, канал: `ibrealistic:settings`
- **Документация:** `CHANGES_obu_addon_refactoring.md`, `REFACTORING_PLAN.md`

---

# ИЗВЕСТНЫЕ БАГИ И ПРОБЛЕМЫ 🐛

## BUG-1: Накопление воздушного состояния ✅ ИСПРАВЛЕНО
**Приоритет:** ВЫСОКИЙ — влияет на геймплей
**Статус:** Исправлено в Фазе 6.1
**Симптомы:**
- Первый прыжок проходит нормально (кроме резкой остановки при приземлении)
- Второй и последующие прыжки тянут машину в сторону
- Эффект накапливается: каждый следующий прыжок хуже предыдущего

**Причина:** `vy` и `fyActual[]` не обнулялись при приземлении; `expectedWorldVx/Vz` не пересчитывался после сброса, создавая ложные коллизии

**Решение:**
- [x] Полный сброс `vy = 0`, `fyActual[i] = 0`, `yawRate *= 0.5` при приземлении после длительного полёта (≥5 тиков)
- [x] Пересчёт `expectedWorldVx/Vz` после сброса
- [x] Добавлен debug HUD (`/realisticdebug`) для диагностики
- [x] Улучшенное прямолинейное демпфирование vy при отсутствии руля

## BUG-2: Резкая остановка при приземлении ✅ ИСПРАВЛЕНО
**Приоритет:** СРЕДНИЙ
**Статус:** Исправлено в Фазе 6.2
**Симптомы:** Машина теряет почти всю скорость при приземлении с прыжка
**Причина:** Vanilla `move()` клиппит горизонтальную скорость при столкновении с землёй
**Решение:** В `moveHook()` сохраняется горизонтальная скорость до `move()`. При приземлении (вертикальная скорость была отрицательной и стала больше) если потеря >30% — восстанавливается 85% в направлении движения. Стеновые коллизии отличаются по forward component <10%.

---

# БУДУЩИЕ ФАЗЫ

## ФАЗА 6: БАГИ И СТАБИЛЬНОСТЬ ФИЗИКИ ✅
> **Приоритет:** КРИТИЧЕСКИЙ — без стабильной физики всё остальное бессмысленно
> **Зависимости:** нет (делать первой)
> **Статус:** ЗАВЕРШЕНА

### 6.1 Исправление воздушной физики ✅
> **Переводы:** Debug HUD реализован клиент-сайд (не требует переводов)

- [x] **Функционал:** Диагностика проблемы накопления воздушного состояния (BUG-1)
  - [x] Добавить debug-режим: `/realisticdebug` — показывает в action bar: `vx=... vy=... yr=... AIR/GND surf=...`
  - [x] Определить точную причину накопления (vy/fyActual[] не обнулялись при приземлении)
- [x] **Исправление:** Полный сброс бокового состояния при приземлении
  - [x] Полный сброс `vy=0`, `fyActual[]=0`, `yawRate*=0.5` при длительном полёте (≥5 тиков)
  - [x] Частичное демпфирование при коротких прыжках (<5 тиков)
  - [x] Пересчёт `expectedWorldVx/Vz` после сброса — предотвращение ложных коллизий
  - [x] Улучшенное прямолинейное демпфирование vy (0.90) при отсутствии руля и малом yawRate

### 6.2 Исправление остановки при приземлении ✅
- [x] **Функционал:** Сохранение продольной скорости при приземлении (BUG-2)
  - [x] Запомнить горизонтальную скорость перед vanilla `move()`
  - [x] Если потеря > 30% при приземлении — восстановить с коэффициентом (0.85)
  - [x] Учитывать направление: если forward component < 10% — это стена, не восстанавливать
  - [x] Работает только когда FourWheelPhysics активен и лодка падала
- [x] **Тестирование:**
  - [x] Прыжок на плоскость — скорость восстанавливается до ~85% (LANDING_SPEED_RESTORE_FACTOR)
  - [x] Прыжок в стену — forwardComponent < 10% → скорость НЕ восстанавливается
  - [x] Прыжок на спуск — скорость растёт (гравитация, восстановление не применяется т.к. нет потери)

### 6.3 Аудит граничных условий физики ✅
- [x] **Функционал:** Проверка стабильности во всех крайних случаях
  - [x] Скорость 0 → нет NaN/Infinity (isStationary + NaN guard)
  - [x] Скорость > 80 м/с → стабильность (MAX_VELOCITY = 100 clamp)
  - [x] yawRate > 10 рад/с → корректное ограничение (MAX_YAW_RATE = 15 clamp)
  - [x] Переход с льда на асфальт на скорости → плавный переход (per-tick detectSurface)
  - [x] Столкновение со стеной на скорости → collision detection + MAX_COLLISION_LATERAL_INJECTION
  - [x] Езда задом (S) → корректная физика (vxAbs prevents division by zero)
  - [x] Одновременно газ + тормоз → friction circle limits both forces

---

## ФАЗА 7: КАСТОМНАЯ СИСТЕМА ВЕРСИОНИРОВАНИЯ СЕРВЕРОВ ✅
> **Приоритет:** ВЫСОКИЙ — нужно для совместимости при добавлении новых фич
> **Зависимости:** Фаза 6 (стабильная физика)

### 7.1 Протокол версионирования реалистичного мода
> **Суть:** Серверы, поддерживающие реалистичный режим, сообщают клиенту свою версию и возможности.
> Клиент адаптируется под сервер: отключает неподдерживаемые функции, предупреждает о несовместимости.

- [x] **Функционал (мод):**
  - [x] Новый пакет `REALISTIC_SERVER_INFO` (server → client):
    - `realisticVersion` (String, e.g. "1.0.5") — версия реалистичного мода на сервере
    - `supportedFeatures` (bitfield/set) — какие фичи поддерживаются
    - `serverName` (String) — имя сервера для отображения
  - [x] Обработка в `ClientboundPackets.java`:
    - Парсинг версии, сравнение с клиентской
    - Сохранение в `IBRealistic.serverRealisticVersion`
    - Список поддерживаемых фич в `IBRealistic.serverFeatures`
  - [x] Уведомление игрока при подключении:
    - «Сервер поддерживает Realistic v1.0.6 (ваш мод: v1.0.6)» — совместимо
    - «Сервер не поддерживает реалистичный режим» — стандартный OBU
  - [x] Feature flags:
    - `FEATURE_FOUR_WHEEL` (четырёхколёсная физика)
    - `FEATURE_WEATHER` (погода)
    - `FEATURE_ECONOMY` (экономика)
    - `FEATURE_SOLO_RACE` (соло-рейсы)
    - `FEATURE_CUSTOM_CARS` (кастомизация)
    - Расширяемый набор для будущих фич
- [x] **Функционал (плагин):**
  - [x] Отправка `REALISTIC_SERVER_INFO` при подключении игрока с OBU
  - [x] Конфигурация в `config.yml`:
    ```yaml
    realistic:
      serverName: "Realistic Rally Server"
      features:
        fourWheel: true
        weather: true
        economy: false
        soloRace: false
        customCars: false
    ```
  - [x] Версия определяется автоматически из plugin metadata (не хардкодится)
  - [x] Graceful degradation: проверка feature flags клиента
- [x] **Пакеты:** Новый packet ID для `REALISTIC_SERVER_INFO` (61)
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: `realistic_version_compatible`, `realistic_not_supported`
  - [x] `lang/triton.yml`: обёртки
  - [x] `triton/timingsystem.json`: все языки (en_GB, ru_RU, de_DE, pl_PL, nl_NL, es_ES, pt_BR, fr_FR)
  - [x] Остальные `lang/*.yml` (de_de, id_id, zh_cn)

### 7.2 Клиентский ответ серверу
- [x] **Функционал (мод):**
  - [x] Пакет `REALISTIC_CLIENT_INFO` (client → server):
    - `clientRealisticVersion` (String) — версия мода на клиенте
    - `clientFeatures` (bitfield) — какие фичи поддерживает мод
  - [x] Отправляется автоматически после получения `REALISTIC_SERVER_INFO`
- [x] **Функционал (плагин):**
  - [x] Обработка `REALISTIC_CLIENT_INFO`
  - [x] Сохранение версии мода игрока в PlayerData (TPlayer)
  - [x] Учёт при отправке настроек (feature flags)

---

## ФАЗА 8: ЭКОНОМИКА И ПРОГРЕССИЯ 💰 ✅
> **Приоритет:** ВЫСОКИЙ — фундамент для кастомизации и мотивации
> **Зависимости:** Фаза 7 (версионирование — чтобы экономика работала только на совместимых серверах)
> **Принцип:** Функционал → данные → GUI. Сначала работающая экономика, потом красивый интерфейс.

### 8.1 Интеграция с Vault — УБРАНО
> Vault убран по решению пользователя. Используется только внутренняя валюта Rally Coins.

### 8.2 Внутренняя валюта (Rally Coins) ✅
> **Зачем:** Отдельная валюта для раллийной системы, не зависящая от общей экономики сервера.
> Может работать параллельно с Vault или вместо него.

- [x] **Функционал (плагин):**
  - [x] `RallyCoinManager.java`:
    - Хранение баланса в БД (таблица `ts_player_coins`)
    - `int getCoins(UUID)` — получить баланс
    - `void addCoins(UUID, int, String reason)` — начислить (с причиной для логов)
    - `boolean spendCoins(UUID, int, String reason)` — потратить (если хватает)
    - `List<CoinTransaction> getHistory(UUID, int limit)` — история транзакций
  - [x] Таблица `ts_player_coins` (Version14 migration)
  - [x] Таблица `ts_coin_transactions` (Version14 migration)
  - [x] Заработок монет (EconomyListener):
    - Завершение трека: 20 монет (настраивается)
    - Первое прохождение трека: ×3 бонус
    - Побитие личного рекорда: +25 монет
    - Попадание в топ-3 лидерборда: +100/+50/+25 монет (настраивается)
  - [x] Конфигурация наград в `config.yml`
- [x] **Команды:**
  - [x] `/coins` — показать баланс
  - [x] `/coins history` — история транзакций
  - [x] `/coins pay <игрок> <сумма>` — перевод другому игроку
  - [x] `/coins admin give <игрок> <сумма>` — админ-начисление
  - [x] `/coins admin take <игрок> <сумма>` — админ-списание
  - [x] `/coins admin set <игрок> <сумма>` — установить баланс
- [x] **Permissions:**
  - [x] `timingsystem.coins.balance` — просмотр баланса (default: true)
  - [x] `timingsystem.coins.pay` — перевод монет (default: true)
  - [x] `timingsystem.coins.admin` — админ-команды (default: op)

### 8.3 Система уровней и XP ✅
> **Суть:** Уровень отображается в XP-баре Minecraft (снизу экрана).
> Уровень определяет доступ к машинам, трекам, и предметам магазина.

- [x] **Функционал (плагин):**
  - [x] `LevelManager.java`:
    - Хранение XP в БД (таблица `ts_player_levels`)
    - `int getLevel(UUID)` — текущий уровень
    - `int getXP(UUID)` — текущий XP
    - `int getXPForLevel(int level)` — XP для достижения уровня
    - `void addXP(UUID, int, String reason)` — начислить XP
    - `float getProgress(UUID)` — прогресс до следующего уровня (0.0–1.0)
  - [x] Таблица `ts_player_levels` (Version15 migration)
  - [x] Формула уровней (прогрессивная): base × N × (1 + growth × (N - 1))
  - [x] Максимальный уровень: 100 (настраивается)
  - [x] Заработок XP через EconomyListener (track complete, record bonus, first completion ×2)
  - [x] Визуальное отображение: XP bar + level number через setLevel()/setExp()
  - [x] Бонусы за уровни (конфигурация unlock_levels в config.yml)
  - [x] Конфигурация в `config.yml`
- [x] **Команды:**
  - [x] `/level` — показать текущий уровень, XP, прогресс
  - [x] `/level top` — топ игроков по уровню
  - [x] `/level admin setlevel <игрок> <уровень>` — установить уровень
  - [x] `/level admin addxp <игрок> <xp>` — начислить XP
- [x] **Permissions:**
  - [x] `timingsystem.level.view` — просмотр уровня (default: true)
  - [x] `timingsystem.level.top` — просмотр топа (default: true)
  - [x] `timingsystem.level.admin` — админ-команды (default: op)

### 8.4 Ежедневные задания (Daily Challenges) ✅
> **Зачем:** Мотивация возвращаться каждый день, разнообразие геймплея.

- [x] **Функционал (плагин):**
  - [x] `DailyChallengeManager.java`:
    - Генерация 3 случайных заданий в день (для всех игроков одинаковые)
    - Отслеживание прогресса
    - Автоматический сброс при смене даты UTC
  - [x] 7 типов заданий (COMPLETE_TRACKS, BEAT_RECORD, COMPLETE_WITHOUT_RESET, COMPLETE_FIVE_TRACKS, FIRST_PLACE, COMPLETE_DIFFERENT_TRACKS, COMPLETE_TEN_TRACKS)
  - [x] Хранение: таблица `ts_daily_challenges` + `ts_player_daily_progress` (Version16)
  - [x] Интеграция с EconomyListener — прогресс обновляется при завершении трека
- [x] **Команды:**
  - [x] `/daily` — показать текущие задания и прогресс
  - [x] `/daily admin regenerate` — пересоздать задания
- [x] **Permissions:**
  - [x] `timingsystem.daily.view` — просмотр заданий (default: true)
  - [x] `timingsystem.daily.admin` — админ-команды (default: op)

### 8.5 Интеграция начислений с TimingSystem ✅
> **Зачем:** Автоматическое начисление монет при прохождении треков.

- [x] **Функционал (плагин):**
  - [x] EconomyListener на `TimeTrialFinishEvent` → начисление монет + XP
  - [x] Listener на `DriverFinishHeatEvent` → начисление за позицию + участие
  - [x] Проверка: первое прохождение? Новый рекорд?
  - [x] Уведомление игрока: «+20 🪙» / «+20 🪙 (+25 record bonus)»
  - [x] XP bar обновляется при входе игрока (TSListener.onPlayerJoin)
  - [ ] Настраиваемые множители наград для каждого трека через `TrackTag` (будущее)

---

## ФАЗА 9: КАСТОМИЗАЦИЯ АВТОМОБИЛЕЙ 🏎️ ✅
> **Приоритет:** ВЫСОКИЙ — ключевая фича для долгосрочной мотивации
> **Зависимости:** Фаза 8 (экономика + уровни — для покупки и разблокировки)
> **Принцип:** Сначала система пресетов → потом сборка → потом магазин GUI
> **Статус:** РЕАЛИЗОВАНО (9.1-9.3 — пресеты, гараж, магазин; 9.4-9.5 — планируется)

### 9.1 Система пресетов компонентов (данные) ✅
> **Суть:** Все компоненты — предопределённые пресеты. Игрок НЕ может задавать произвольные числа.

- [x] **Функционал (мод — physics/):**
  - [x] `TirePreset.java` — 7 пресетов (gripMultiplier, slideMultiplier, relaxationMultiplier, loadSensitivityMod)
  - [x] `EnginePreset.java` — 5 пресетов (engineForceMultiplier, engineBrakingMultiplier, dragMultiplier)
  - [x] `BodyPreset.java` — 5 пресетов (massMultiplier, downforceMultiplier, dragMultiplier)
  - [x] `SuspensionPreset.java` — 4 пресета (rollStiffnessRatio, cgHeightMultiplier, yawRateDampingMultiplier)
  - [x] `SteeringPreset.java` — 4 пресета (maxSteeringAngle, steeringSpeed, steeringReturnRate, speedSteeringFactor)
  - [x] `BrakePreset.java` — 4 пресета (brakingForceMultiplier, brakeBias)
  - [x] `WeightDistributionPreset.java` — 4 пресета (frontWeightBias)
  - [x] Каждый пресет имеет requiredLevel и price
- [x] **Интеграция (мод):**
  - [x] VehicleConfig — preset fields + 13 effective getters
  - [x] FourWheelPhysicsEngine — использует effective values
  - [x] ClientboundPackets — пакеты 62-68
  - [x] SingleplayerCommands — 7 команд для тестирования
- [x] **Интеграция (плагин):**
  - [x] CustomBoatUtilsMode — PACKET_ID, поля, applyToPlayer(), getNonDefaultSettings()
  - [x] CommandBoatUtilsModeEdit — set-команды для пресетов

### 9.2 Гараж игрока (PlayerGarage) ✅
> **Суть:** У каждого игрока есть «гараж» — набор собранных машин.

- [x] **Функционал (плагин):**
  - [x] `PlayerCar.java` (DTO): id, ownerUuid, name, 8 preset полей, active флаг, JSON сериализация
  - [x] `GarageManager.java`: CRUD, lookup пресетов, цены, уровни, applyCarToMode()
  - [x] Таблица `ts_player_garage` (Version17 миграция, DB v17)
  - [x] Максимум 5 машин на игрока

### 9.3 Система покупок (без GUI — только команды) ✅
> **Порядок:** Сначала команды, потом GUI (Фаза 16)

- [x] **Команды:**
  - [x] `/garage` — список машин в гараже
  - [x] `/garage select <id>` — выбрать активную машину
  - [x] `/garage create <имя>` — создать новую машину
  - [x] `/garage delete <id>` — удалить машину
  - [x] `/garage info <id>` — подробная информация о машине
  - [x] `/garage upgrade <id> <компонент> <пресет>` — улучшить компонент (с проверкой уровня и монет)
  - [x] `/shop [категория]` — список доступных пресетов с ценами
- [x] **Логика покупки:**
  - [x] Проверка уровня (LevelManager)
  - [x] Проверка баланса (RallyCoinManager)
  - [x] Списание монет при покупке
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: garage.*, shop.* сообщения

### 9.4 Спавн автомобиля из гаража ✅
> **Суть:** Хотбар-предмет «машина» спавнит лодку с настройками активной машины из гаража.

- [x] **Функционал (плагин):**
  - [x] Хотбар-предмет CHERRY_BOAT показывает имя активной машины из гаража (SpawnManager.giveHotbarItems)
  - [x] Lore: краткие характеристики (componentSummary)
  - [x] При спавне лодки: применяются пресеты из гаража (ApiUtilities.applyGarageCarPresets)
  - [x] Если у игрока нет машины в гараже — спавн стандартной лодки с системными настройками трека

### 9.5 Системные настройки (для треков без кастомизации) ✅
> **Суть:** Некоторые треки используют фиксированные настройки для всех игроков (fair play).
> Игрок всегда может выбрать: играть на своей машине ИЛИ на системных настройках.

- [x] **Функционал (плагин):**
  - [x] TrackOption ALLOW_GARAGE_CARS (id: 10) — если включено, игроки могут использовать свою машину
  - [x] Без ALLOW_GARAGE_CARS: применяется CustomBoatUtilsMode трека (как сейчас)
  - [x] С ALLOW_GARAGE_CARS: пресеты из гаража применяются поверх настроек трека
  - [x] Настройка через существующий `/ts trackedit option ALLOW_GARAGE_CARS`

---

## ФАЗА 10: СИСТЕМА ГОНОК (СОЛО + МУЛЬТИПЛЕЕР) 🏁
> **Приоритет:** ВЫСОКИЙ — уникальная фича для сервера
> **Зависимости:** Фаза 9 (кастомизация — выбор машины), Фаза 8 (уровни — доступ к машинам)
> **Суть:** Единая система гонок с поддержкой как соло-режима (изоляция, без коллизий),
> так и мультиплеерного режима (совместные гонки на одном треке).

### 10.1 Система изоляции игроков (Соло-режим)
- [x] **Функционал (плагин):**
  - [x] `SoloRaceManager.java`:
    - Управление активными рейсами (соло + мультиплеер)
    - Изоляция через `Player.hideEntity()` API (видимость)
    - Состояния: WAITING → COUNTDOWN → RACING → FINISHED / CANCELLED
  - [x] `RaceSession.java` — сессия отдельного игрока в гонке
  - [x] `RaceState.java` — состояния: WAITING, COUNTDOWN, RACING, FINISHED, CANCELLED
  - [x] `RaceType.java` — SOLO / MULTIPLAYER
  - [x] При старте соло-рейса:
    1. Скрыть всех других игроков и лодки (`player.hideEntity(other)`)
    2. Телепортировать на стартовую позицию
    3. Применить конфигурацию машины
    4. Начать обратный отсчёт с Title + звуком
  - [x] При завершении / отмене:
    1. Показать всех игроков обратно
    2. Восстановить видимость через LonelinessController
    3. Начислить награды (монеты + XP)
  - [x] Конфигурация:
    ```yaml
    race:
      enabled: true
      max_concurrent: 20          # Макс. одновременных рейсов
      countdown_seconds: 5         # Обратный отсчёт
      timeout_minutes: 10          # Тайм-аут (авто-отмена)
      rewards:
        coins: 30
        xp: 40
    ```

### 10.1.1 Мультиплеерный режим
- [x] **Функционал (плагин):**
  - [x] Мультиплеерные гонки используют существующую систему QuickRace (Event/Round/Heat)
  - [x] Команды `/race create`, `/race join`, `/race start` — мультиплеер через Heat
  - [x] `/race solo <трек>` — индивидуальная гонка с изоляцией
  - [x] `startMultiplayerRace()` создаёт RaceSession для каждого участника
  - [x] Добавлен `MULTIPLAYER_RACE` feature flag (bit 5) в RealisticFeature

### 10.2 Выбор машины для гонки
- [x] **Функционал (плагин):**
  - [x] При старте рейса игрок выбирает:
    - **Своя машина** (из гаража, если есть) — `/race solo <трек> custom`
    - **Системная машина** (настройки трека, по умолчанию) — `/race solo <трек>`
  - [x] Если у игрока нет машины в гараже → автоматически системная
  - [x] Результаты фильтруются по типу машины (SYSTEM / CUSTOM)

### 10.3 Лидерборды и результаты
- [x] **Функционал (плагин):**
  - [x] Фильтрация результатов по типу машины (system/custom)
  - [x] Таблица `ts_race_results` (DB version 18):
    ```sql
    CREATE TABLE ts_race_results (
      id INT AUTO_INCREMENT PRIMARY KEY,
      uuid VARCHAR(36),
      track_id INT,
      time_ms BIGINT,
      race_type VARCHAR(16) DEFAULT 'SOLO',
      car_type VARCHAR(16) DEFAULT 'SYSTEM',
      created_at VARCHAR(255)
    );
    ```
  - [x] Команды просмотра: `/race results <трек> [system|custom]`, `/race top <трек> [system|custom]`

### 10.4 Стартовая процедура
- [x] **Функционал (плагин):**
  - [x] Обратный отсчёт: 5... 4... 3... 2... 1... GO!
  - [x] Визуально: Title на экране с числами + звук (NOTE_BLOCK_HAT → PLING)
  - [x] При `GO!` — время начинается
  - [x] Тайм-аут: авто-отмена через configurable минуты

### 10.5 Команды
- [x] **Команды:**
  - [x] `/race solo <трек> [system|custom]` — начать соло-рейс
  - [x] `/race cancel` — отменить текущий рейс
  - [x] `/race results <трек> [system|custom]` — результаты по треку
  - [x] `/race top <трек> [system|custom]` — топ-10 по треку
  - [x] `/race create <трек> [кругов] [питов]` — мультиплеерная гонка (QuickRace)
  - [x] `/race join` — присоединиться к мультиплеерной гонке
  - [x] `/race start` — начать мультиплеерную гонку
  - [x] `/race leave` — покинуть гонку (соло или мультиплеер)
- [x] **Permissions:**
  - [x] `ts.race.solo` — запуск соло-рейса (default: true)
  - [x] `ts.race.results` — просмотр результатов (default: true)
- [x] **Переводы:**
  - [x] `lang/en_us.yml` + `lang/de_de.yml` + `lang/triton.yml`
  - [x] `triton/timingsystem.json` (все 8 языков)

---

## ФАЗА 11: НАСТРОЙКИ ТРЕКОВ (ПОГОДА, ВРЕМЯ СУТОК) 🌦️
> **Приоритет:** СРЕДНИЙ — значительно улучшает разнообразие
> **Зависимости:** Фаза 10 (соло-рейсы — треки уже должны полноценно работать)

### 11.1 Настройка погоды для треков
> **Суть:** Каждый трек может иметь свою настройку погоды. При входе на трек погода меняется для игрока.
> Погода влияет на физику через существующий WeatherCondition.

- [x] **Функционал (плагин):**
  - [x] Новое поле в Track/TrackData: `WeatherCondition weather = CLEAR`
  - [x] При старте заезда:
    1. Отправить пакет `SET_WEATHER_CONDITION` с погодой трека
    2. Изменить клиентскую погоду для игрока: `player.setPlayerWeather(WeatherType.DOWNFALL)` для дождя/снега
    3. Визуальный эффект: частицы дождя, тумана
  - [x] При выходе с трека: сброс на серверную погоду
  - [x] Доступные значения:
    - `CLEAR` — ясно (по умолчанию)
    - `RAIN` — дождь (grip ×0.70)
    - `HEAVY_RAIN` — ливень (grip ×0.50)
    - `SNOW` — снег (grip ×0.40)
    - `FOG` — туман (grip ×0.95, но видимость снижена)
  - [x] Команда: `/te weather RAIN`

### 11.2 Настройка времени суток для треков
> **Суть:** Каждый трек может иметь своё фиксированное время суток.

- [x] **Функционал (плагин):**
  - [x] Новое поле в Track: `Integer trackTime = null` (null = серверное время)
  - [x] При входе на трек: `player.setPlayerTime(trackTime, false)` — фиксированное время
  - [x] При выходе: `player.resetPlayerTime()` — серверное время
  - [x] Пресеты:
    - `DAWN` — рассвет (0 тиков)
    - `NOON` — полдень (6000 тиков)
    - `SUNSET` — закат (12000 тиков)
    - `NIGHT` — ночь (18000 тиков)
    - `MIDNIGHT` — полночь (13000 тиков)
    - Произвольное число тиков (0–24000)
  - [x] Команда: `/te time NIGHT` или `/te time 15000`

### 11.3 Динамическая погода (опционально)
> **Суть:** Погода может меняться во время заезда (если включено).

- [x] **Функционал (плагин):**
  - [x] Поле трека: `dynamicWeather = false`
  - [x] Если `dynamicWeather=true`:
    - Погода меняется каждые N минут (настраиваемо)
    - Цикл: CLEAR → RAIN → HEAVY_RAIN → RAIN → CLEAR
    - Все игроки в рейсах на треке получают пакет `SET_WEATHER_CONDITION`
    - Уведомление: «⛈ Weather changed to: Rain»
  - [x] Конфигурация:
    ```yaml
    dynamic_weather:
      change_interval_minutes: 5
    ```
  - [x] Команда: `/te dynamicweather enable|disable`
  - [x] DynamicWeatherManager — планировщик с BukkitTask

### 11.4 Сложность трека
> **Суть:** Система оценки сложности трека для начисления наград.

- [x] **Функционал (плагин):**
  - [x] Поле трека: `difficulty` (1–5 звёзд)
  - [x] Влияние на награды:
    ```
    ★☆☆☆☆ (Easy):     ×1.0 монет, ×1.0 XP
    ★★☆☆☆ (Medium):   ×1.3 монет, ×1.2 XP
    ★★★☆☆ (Hard):     ×1.6 монет, ×1.5 XP
    ★★★★☆ (Expert):   ×2.0 монет, ×1.8 XP
    ★★★★★ (Master):   ×2.5 монет, ×2.2 XP
    ```
  - [x] Оценка ручная (через команду)
  - [x] Команда: `/te difficulty 3`

### 11.5 Информация о треке
> **Суть:** Расширенная информация, отображаемая при выборе трека.

- [x] **Функционал (плагин):**
  - [x] Расширить TrackInfo:
    - Название, автор, сложность (★★★☆☆)
    - Погода, время суток
    - Тип машины (системная / свободный выбор)
    - Лучшее время (свой рекорд + мировой рекорд)
    - Награда (монеты + XP с учётом множителей)
  - [x] Отображение в chat при `/track info <трек>`
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: `track_weather`, `track_time_of_day`, `track_difficulty`, `track_best_time`, `track_world_record`, `track_reward`
  - [x] triton.yml + timingsystem.json (все 8 языков)

---

## ФАЗА 12: РАЛЛИЙНЫЕ ПРОЦЕДУРЫ ✅
> **Приоритет:** СРЕДНИЙ — после стабилизации базовых фич
> **Зависимости:** Фаза 11 (настройки треков), Фаза 10 (соло-рейсы)
> **Статус:** ЗАВЕРШЕНА (PR #49)

### 12.1 Стартовая процедура ралли ✅
- [x] **Функционал (плагин):**
  - [x] Цветная система через chat messages (красные 🔴 → жёлтые 🟡 → зелёные ✦ GO! ✦)
  - [x] Обратный отсчёт: 5... 4... 3... 2... 1... GO!
  - [x] Звуки: бип на каждую секунду, длинный бип на GO
  - [x] Блокировка движения во время отсчёта (заморозка walk speed)
  - [x] Настраиваемый countdown в config.yml

### 12.2 Фальстарт ✅
- [x] **Функционал:**
  - [x] Детекция движения во время отсчёта (горизонтальная дистанция > 0.5 блоков)
  - [x] Ступенчатые штрафы: 10с → 60с+рестарт → DQ
  - [x] Уведомление игроку о фальстарте и штрафах
  - [x] Автоматический рестарт при 2-м фальстарте
  - [x] Конфигурация: `race.false_start.enabled`

### 12.3 Time Controls (временные контроли) ✅
> **Суть:** Контрольные точки с допустимым временем прибытия.

- [x] **Функционал:**
  - [x] Новый тип региона TIMECONTROL в TrackRegion.RegionType
  - [x] Допустимое время прибытия: target ± окно (настраиваемое)
  - [x] Штрафы: 10 сек за каждую минуту опоздания, 1 мин за каждую минуту раннего прибытия
  - [x] Отображение штрафов при финише (сводка: raw time + penalties = total)
  - [x] Конфигурация: `race.time_control.*`

### 12.4 Сервис-парк ✅
- [x] **Функционал:**
  - [x] Зоны сервиса на трассе (новый RegionType SERVICEPARK)
  - [x] Ограниченное время обслуживания (настраиваемое, автовыброс)
  - [x] Восстановление характеристик машины
  - [x] Отслеживание состояния через RaceSession

### 12.5 Recce режим (разведка трассы) ✅
- [x] **Функционал:**
  - [x] RecceSession / RecceManager — режим проезда трассы на сниженной скорости
  - [x] Настраиваемый множитель скорости (по умолчанию 50%)
  - [x] Система pace notes: `/race recce note <текст>` — сохранение заметок
  - [x] Команды: `/race recce <трек>`, `/race recce end`, `/race recce note`

### 12.6 Мультиэтапное ралли ✅
- [x] **Функционал:**
  - [x] RallyEvent, RallyStage, RallyParticipant, RallyEventState — модель данных
  - [x] RallyManager — полный жизненный цикл: создание, прогрессия этапов, суперралли (сход с 5мин штрафом)
  - [x] CommandRally — 9 субкоманд: create, addstage, removestage, start, join, retire, results, info, delete
  - [x] DB миграция Version20: таблицы ts_rally_events + ts_rally_stages (MySQL + SQLite)
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: rally.* — все процедурные сообщения
  - [x] Все языки + triton

---

## ФАЗА 13: СИСТЕМА ПОЛОМОК И ИЗНОСА 🔧 ✅
> **Приоритет:** НИЗКИЙ — добавляет глубину, но не критичен
> **Зависимости:** Фаза 9 (кастомизация — нужны компоненты для износа), Фаза 12 (сервис-парк)
> **Статус:** ВЫПОЛНЕНО (2026-02-15)

### 13.1 Износ шин
- [x] **Функционал (мод + плагин):**
  - [x] Новое состояние в движке: `tireWear[4]` (0.0 = новые, 1.0 = изношены)
  - [x] Деградация μ: `muEffective = mu × (1.0 - 0.3 × tireWear)` (30% потеря при полном износе)
  - [x] Скорость износа зависит от:
    - Поверхности (гравий/грязь изнашивает быстрее)
    - Стиля езды (блокировка колёс, резкие повороты)
    - Пресета шин (Soft = быстрый износ, Hard = медленный)
  - [x] Визуальное уведомление: «⚠️ Шины изношены на 70%!»
  - [x] Пакет `SYNC_TIRE_WEAR` (ID 71, server → client)
  - [x] Отображение в HUD (под прицелом): полоски износа каждой шины

### 13.2 Перегрев двигателя
- [x] **Функционал (мод + плагин):**
  - [x] Состояние: `engineTemp` (0.0–1.0, где 1.0 = перегрев)
  - [x] Нагрев: при полном газе в течение 30+ секунд
  - [x] Охлаждение: при отпускании газа, при низкой скорости
  - [x] При перегреве: `engineForce × (1.0 - 0.5 × overheat)` (50% потеря мощности)
  - [x] Визуальное уведомление: «🌡️ Двигатель перегрет!»
  - [x] Индикатор температуры в HUD (цветовой градиент)

### 13.3 Повреждения от столкновений
- [x] **Функционал (мод + плагин):**
  - [x] Состояние: `bodyDamage` (0.0–1.0)
  - [x] Увеличивается при столкновении (серверный расчёт по потере скорости)
  - [x] Эффекты повреждений:
    - `damage < 0.3`: нет эффектов
    - `damage 0.3–0.6`: снижение maxSteering на 10%
    - `damage 0.6–0.8`: снижение engineForce на 20%
    - `damage > 0.8`: критические повреждения — скорость ограничена 50%
  - [x] Визуальные эффекты (частицы): дым при damage > 0.5, искры при столкновении

### 13.4 Ремонт в сервис-зонах
- [x] **Функционал:**
  - [x] При въезде в зону сервиса — начинается ремонт (автоматически)
  - [x] Постепенное восстановление: шин, двигателя, кузова
  - [x] Визуальный прогресс-бар ремонта в HUD
  - [x] API для setServiceZone() из серверных событий
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: 14 новых ключей damage.*
  - [x] Все 9 языков + triton.yml + triton/timingsystem.json

---

## ФАЗА 14: АНТИЧИТ И СЕРВЕРНАЯ ВАЛИДАЦИЯ 🛡️
> **Приоритет:** СРЕДНИЙ — важно для конкурентного геймплея
> **Зависимости:** Фаза 8 (экономика), Фаза 9 (кастомизация), полная физика

### 14.1 Серверная валидация скорости
- [x] **Функционал (плагин):**
  - [x] `AntiCheatManager.java`:
    - Проверка скорости каждые N тиков
    - Максимально допустимая скорость для каждого типа машины/пресета
    - Допуск для сетевых задержек (+20%)
    - Допуск для прыжков и падений (+30% вертикальная)
  - [x] Действия при нарушении:
    - Предупреждение (1-й раз)
    - Телепорт на последнюю валидную позицию (2-й раз)
    - Дисквалификация с рейса (3-й раз)
    - Уведомление админам
  - [x] Конфигурация:
    ```yaml
    anticheat:
      enabled: true
      check_interval_ticks: 10
      speed_tolerance: 1.2
      max_violations: 3
      notify_admins: true
      log_violations: true
    ```
  - [x] **Дополнительные меры безопасности:**
    - Детекция аномальной акселерации (невозможное ускорение)
    - Детекция телепортации (перемещение на большие расстояния)
    - Отслеживание последней безопасной позиции (lastSafeLocation)
    - Кулдаун после дисквалификации (30 секунд)
    - Логирование нарушений в БД (ts_anticheat_violations)

### 14.2 Валидация конфигурации машины
- [x] **Функционал:**
  - [x] Проверка: пресеты машины игрока существуют и разблокированы
  - [x] Проверка: уровень не подделан (серверная проверка)
  - [x] При несоответствии: сброс на системные настройки

### 14.3 Защита экономики
- [x] **Функционал:**
  - [x] Rate limiting: максимум N транзакций в минуту
  - [x] Лимит переводов: максимум M монет в день
  - [x] Детекция дюпов: проверка суммы монет в системе (auditCoinSupply)
  - [x] Логирование всех транзакций с причинами
  - [x] **Дополнительные меры:** Проверка максимальной суммы одной транзакции
- [x] **Переводы:**
  - [x] `lang/en_us.yml`: `anticheat.warning`, `anticheat.violation`, `anticheat.disqualified`
  - [x] Все языки + triton

---

## ФАЗА 15: ВИЗУАЛ, GUI И ПОЛИРОВКА ✨
> **Приоритет:** НИЗКИЙ — делать после всего функционала
> **Зависимости:** ВСЕ предыдущие фазы (функционал должен работать до визуала)
> **ПРАВИЛО: Нельзя делать GUI без готового бэкенда!**

### 15.1 GUI магазина (Chest Menu) ✅
> **Зависимость:** Фаза 9.3 (покупки через команды уже работают)

- [x] **Функционал (плагин):**
  - [x] `ShopGui.java` — 54-слотовый инвентарь:
    - Ряд 1: категории (Шины / Двигатели / Кузова / Подвеска / Рулевое / Тормоза / Вес / Тип)
    - Ряды 2-5: пресеты категории
    - Ряд 6: навигация (назад, баланс, уровень)
  - [x] Каждый предмет:
    - Название пресета (зелёное = куплено, жёлтое = доступно, красное = заблокировано)
    - Lore: характеристики, цена, требуемый уровень
    - При клике: покупка → применение
  - [x] Звуки при покупке (ENTITY_PLAYER_LEVELUP)
  - [x] `/shop` → открывает GUI вместо текстового вывода

### 15.2 GUI гаража (Chest Menu) ✅
> **Зависимость:** Фаза 9.2 (гараж через команды уже работает)

- [x] **Функционал (плагин):**
  - [x] `GarageGui.java` — 54-слотовый инвентарь:
    - Слоты 0-4: машины игрока (текущая с glow, пустые = «+» для создания)
    - Слоты 9-16: компоненты выбранной машины (тип, шины, двигатель, кузов, подвеска, рулевое, тормоза, вес)
    - Слоты 45-53: действия (вернуться, выбрать активную, удалить, открыть магазин, баланс)
  - [x] `/garage` → открывает GUI вместо текстового вывода

### 15.3 GUI профиля игрока ✅
> **Зависимость:** Фаза 8 (экономика + уровни работают)

- [x] **Функционал (плагин):**
  - [x] `ProfileGui.java` — 4-рядный инвентарь:
    - Голова игрока с именем
    - Уровень, XP, прогресс-бар
    - Баланс монет, заработано/потрачено
    - Статистика: пройдено треков, финиши, время
    - Текущая машина
  - [x] `/profile` → открывает свой профиль
  - [x] `/profile <игрок>` → просмотр профиля другого игрока
  - [x] CommandProfile зарегистрирован в TimingSystem

### 15.4 GUI ежедневных заданий ✅
> **Зависимость:** Фаза 8.4 (задания через команды работают)

- [x] **Функционал:**
  - [x] `DailyGui.java` — 3-рядный инвентарь:
    - 3 задания с прогресс-барами
    - Награды за каждое задание
    - Таймер до сброса заданий (часы)
  - [x] `/daily` → открывает GUI вместо текстового вывода

### 15.5 GUI выбора трека (расширение) ✅
> **Зависимость:** Фаза 11 (настройки треков)

- [x] **Функционал:**
  - [x] Расширенный TrackGui:
    - Показывает погоду (иконка), сложность (звёзды)
    - Показывает свой рекорд и мировой рекорд (топ-1 время + имя)
    - Показывает награду (монеты + XP с учётом множителя сложности)

### 15.6 Визуальные эффекты (мод) ✅
> **Зависимость:** Физика полностью стабильна

- [x] **Функционал (мод):**
  - [x] Частицы пыли/грязи от колёс (зависят от поверхности) — VehicleParticleRenderer
    - Гравий/песок: дымчатые частицы (CAMPFIRE_COSY_SMOKE)
    - Грязь/глина: частицы мицелия (MYCELIUM)
    - Снег: снежинки (SNOWFLAKE)
  - [x] Брызги воды при езде по мокрой поверхности (SPLASH на asphalt_wet и mud)
  - [x] Дым при блокировке колёс (handbrake на асфальте) и заносе (drift detection)
  - [x] Интенсивность зависит от скорости (3 порога)
  - [x] Искры при столкновении (уже реализовано в DamageParticleRenderer)
  - [ ] HUD: спидометр, тахометр (вынесено в backlog — требует отдельный renderer)

### 15.7 Звуковые эффекты ✅
> **Зависимость:** Физика полностью стабильна

- [x] **Функционал (мод — VehicleSoundRenderer):**
  - [x] Двигатель: minecart rolling, pitch зависит от скорости (0.5-2.0)
  - [x] Визг шин (grindstone sound) — при заносе/handbrake на асфальте
  - [x] Удар при столкновении (iron golem hurt) — с cooldown 20 тиков
  - [x] Гравий/грунт/снег — соответствующие step sounds при езде
  - [x] Cross-version compatible (world.playSound API)

### 15.8 Scoreboard / BossBar ✅
- [x] **Функционал (плагин — RaceBossBarManager):**
  - [x] BossBar с информацией о текущем заезде:
    - Трек, текущее время, погода, машина
    - Цвет по прогрессу: зелёный (впереди PB), жёлтый (близко), красный (позади)
  - [x] Автоматическое show при старте time trial, hide при завершении/отмене
  - [x] Обновление каждые 5 тиков
  - [x] Sidebar scoreboard: уже существует (TimeTrialScoreboard)

### 15.9 Кастомные модели машин (Resource Pack)
> **Приоритет:** Будущее — самая последняя задача

- [ ] **Функционал:**
  - [ ] Custom model data для каждого VehicleType
  - [ ] Resource pack с 3D моделями машин
  - [ ] Интеграция с ItemsAdder / Oraxen (опционально)
  - [ ] Замена визуала лодки на модель машины через Resource Pack

---

## ФАЗА 17: ТЕЛЕМЕТРИЯ И ВАЛИДАЦИЯ РЕЙСОВ 📊
> **Приоритет:** КРИТИЧЕСКИЙ — основа для ghost racing, аналитики, серверного античита
> **Зависимости:** Фаза 10 (рейсы), Фаза 14 (античит)
> **Принцип:** Каждый рейс ОБЯЗАН иметь валидированную телеметрию.
> Без телеметрии рейс = невалидный. Клиент сохраняет данные локально + отправляет на сервер.

### 17.1 Клиентская запись телеметрии (Мод)
> **Суть:** Мод записывает полные данные каждого заезда каждый тик. Все float-значения
> округляются до 3 знаков после запятой для оптимального баланса точности и размера.

- [ ] **Функционал (мод):**
  - [ ] `TelemetryRecorder.java` (новый класс в `dev.kanorto.ibrealistic.telemetry`):
    - Начало записи: при старте рейса (получение пакета START_RACE / команда /race solo)
    - Конец записи: при финише, отмене, выходе с сервера
    - Запись каждый тик (50 мс, ~20 Hz):
      ```
      Структура TelemetryFrame (фиксированный размер):
      ─ tick (int)                      — номер тика с начала рейса
      ─ posX, posY, posZ (float×3)      — мировая позиция (3 знака)
      ─ vx, vy (float×2)               — скорость в локальной системе (3 знака)
      ─ yawAngle (float)               — угол поворота (3 знака)
      ─ yawRate (float)                — скорость вращения (3 знака)
      ─ steeringAngle (float)          — угол руля (3 знака)
      ─ throttleInput (float)          — газ 0.000–1.000
      ─ brakeInput (float)             — тормоз 0.000–1.000
      ─ handbrake (boolean)            — ручник
      ─ airborne (boolean)             — в воздухе
      ─ surfaceType (byte)             — тип поверхности (ordinal)
      ─ slipAngleFL, slipAngleFR,
        slipAngleRL, slipAngleRR (float×4) — углы скольжения колёс (3 знака)
      ─ speedKmh (float)               — скорость км/ч (3 знака)
      ─ gForceLateral (float)          — боковая перегрузка G (3 знака)
      ─ gForceLongitudinal (float)     — продольная перегрузка G (3 знака)
      ```
    - Размер одного фрейма: ~80 байт
    - 5-минутный рейс ≈ 6000 тиков × 80 байт = ~480 КБ (до сжатия)
  - [ ] Утилита округления: `roundTo3(float value)` = `Math.round(value * 1000f) / 1000f`
  - [ ] Метаданные записи (TelemetryHeader):
    ```
    ─ version (int)                  — версия формата телеметрии (1)
    ─ playerUUID (UUID)              — UUID игрока
    ─ playerName (String)            — имя игрока
    ─ trackId (int)                  — ID трека (0 для одиночной игры)
    ─ raceType (byte)                — SOLO / MULTIPLAYER
    ─ carType (byte)                 — SYSTEM / CUSTOM
    ─ vehicleType (byte)             — VehicleType ordinal
    ─ startTimestamp (long)          — Unix timestamp начала
    ─ totalTicks (int)               — общее кол-во тиков
    ─ finishTimeMs (long)            — время финиша в мс (0 = не финишировал)
    ─ checksum (long)                — CRC64 хэш данных для проверки целостности
    ```
  - [ ] Кольцевой буфер: максимум MAX_TELEMETRY_TICKS = 24000 тиков (20 минут)
    - При превышении: старые данные отбрасываются (FIFO)
    - Уведомление: «⚠ Телеметрия: превышен лимит записи»

### 17.2 Локальное сохранение на клиенте (Мод)
> **Суть:** ВСЕ рейсы сохраняются в файл на клиенте, даже если не были отправлены на сервер.
> Файлы хранятся в `.minecraft/ibrealistic/telemetry/`. Это бэкап на случай потери соединения.

- [ ] **Функционал (мод):**
  - [ ] `TelemetryFileManager.java` (новый класс):
    - Директория: `.minecraft/ibrealistic/telemetry/{server_ip_hash}/`
    - Формат файлов: бинарный `.ibrt` (IBRealistic Telemetry)
      ```
      Структура файла (.ibrt):
      ─ Magic bytes: "IBRT" (4 байта)
      ─ Format version: 1 (int)
      ─ TelemetryHeader (см. выше)
      ─ GZIP-сжатый блок:
        ─ TelemetryFrame[totalTicks]
      ─ CRC64 checksum (long)
      ```
    - Именование: `{timestamp}_{trackId}_{result}.ibrt`
      - Пример: `20260216_153000_track42_finished.ibrt`
      - Пример: `20260216_160000_track7_cancelled.ibrt`
    - Автоматическое удаление файлов старше 30 дней (настраиваемо)
    - Максимум 500 файлов (настраиваемо, самые старые удаляются)
  - [ ] Сохранение происходит:
    - При финише рейса (автоматически)
    - При отмене рейса (автоматически)
    - При дисконнекте (автоматически, текущий буфер)
  - [ ] Повторная отправка: если файл не был отправлен на сервер → помечается как `pending`
    - При следующем подключении к серверу: автоматическая отправка pending файлов
    - Команда `/telemetry resend` — ручная повторная отправка

### 17.3 Отправка телеметрии на сервер (Мод → Плагин)
> **Суть:** После финиша клиент отправляет GZIP-сжатую телеметрию на сервер.
> Большие пакеты фрагментируются. Сервер подтверждает приём.

- [ ] **Функционал (мод → плагин):**
  - [ ] Новые пакеты на канале `ibrealistic:settings` (client → server):
    - `TELEMETRY_START` (ID: 80): начало передачи (header + totalChunks + totalBytes)
    - `TELEMETRY_CHUNK` (ID: 81): фрагмент данных (chunkIndex, chunkData[])
    - `TELEMETRY_END` (ID: 82): конец передачи (checksum)
  - [ ] Новые пакеты на канале `ibrealistic:settings` (server → client):
    - `TELEMETRY_ACK` (ID: 83): подтверждение приёма (status: OK / RETRY_CHUNK / REJECT)
    - `TELEMETRY_RESULT` (ID: 84): результат валидации (VALID / INVALID + reason)
  - [ ] Параметры передачи:
    - Размер чанка: 16 КБ (настраиваемо, ≤32 КБ — лимит Minecraft)
    - Таймаут: 30 секунд на всю передачу
    - Ретраи: до 3 попыток при потере чанков
    - GZIP сжатие: ~60-70% экономия (480 КБ → ~150 КБ)
  - [ ] Очередь отправки: если несколько рейсов быстро → очередь с приоритетом
  - [ ] Отправка начинается через 1 секунду после финиша (даём серверу обработать результат)

### 17.4 Приём и хранение телеметрии (Плагин)
> **Суть:** Сервер принимает, собирает из чанков и сохраняет телеметрию.

- [ ] **Функционал (плагин):**
  - [ ] `TelemetryReceiver.java` (новый класс):
    - Приём чанков, сборка в единый byte[]
    - Проверка CRC64 checksum
    - Декомпрессия GZIP
    - Десериализация фреймов
    - Таймаут сессии: если не все чанки за 30 секунд → отмена
  - [ ] `TelemetryStorage.java` (новый класс):
    - Хранение: файловая система `plugins/TimingSystem/telemetry/{uuid}/`
    - Формат: `.ibrt` (тот же, что на клиенте)
    - Именование: `{raceId}_{trackId}_{timestamp}.ibrt`
    - Автоудаление: файлы старше 90 дней (настраиваемо)
    - Максимум: 100 файлов на игрока (настраиваемо, старые → удалить)
  - [ ] Метаданные в БД (новая таблица `ts_telemetry_meta`):
    ```sql
    CREATE TABLE ts_telemetry_meta (
      id INT AUTO_INCREMENT PRIMARY KEY,
      uuid VARCHAR(36) NOT NULL,
      race_result_id INT,                    -- ссылка на ts_race_results
      track_id INT NOT NULL,
      file_path VARCHAR(255) NOT NULL,       -- путь к .ibrt файлу
      total_ticks INT NOT NULL,
      finish_time_ms BIGINT,
      validation_status VARCHAR(16) DEFAULT 'PENDING',  -- PENDING/VALID/INVALID
      validation_reason VARCHAR(255),
      avg_speed_kmh FLOAT,
      max_speed_kmh FLOAT,
      drift_percent FLOAT,                   -- % времени в заносе
      checksum BIGINT NOT NULL,
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
      INDEX idx_uuid (uuid),
      INDEX idx_track (track_id),
      INDEX idx_status (validation_status)
    );
    ```
  - [ ] DB миграция: Version22 (автоматическая, SQLite + MySQL)

### 17.5 Серверная валидация телеметрии (Плагин)
> **КРИТИЧЕСКИ ВАЖНО:** Если сервер не получает телеметрию или она странная — рейс НЕВАЛИДНЫЙ.
> Все рейсы ОБЯЗАНЫ иметь валидированную телеметрию для засчитывания.

- [ ] **Функционал (плагин):**
  - [ ] `TelemetryValidator.java` (новый класс):
    - **Обязательная валидация** (INVALID при нарушении):
      1. **Наличие данных:** телеметрия получена и не пустая
      2. **Целостность:** CRC64 checksum совпадает
      3. **Длительность:** кол-во тиков ≈ время рейса (±10%)
      4. **Стартовая позиция:** первый фрейм ≈ стартовая позиция трека (±5 блоков)
      5. **Финишная позиция:** последний фрейм ≈ финишный регион трека
      6. **Непрерывность:** нет телепортов (расстояние между фреймами ≤ maxPossibleSpeed × tickTime)
         - maxPossibleSpeed = настроенный лимит + tolerance (1.5×)
    - **Физическая валидация** (INVALID при нарушении):
      7. **Ускорение:** дельта скорости между фреймами ≤ максимально возможное ускорение × 1.3
      8. **Скорость поворота:** yawRate не превышает физически возможный (с учётом скорости и руля)
      9. **Скорость:** speedKmh ≤ максимальная для типа машины × 1.2
    - **Статистическая валидация** (SUSPICIOUS → ручная проверка):
      10. **Средняя скорость:** не слишком высокая для трека (сравнение с историей)
      11. **Процент полного газа:** > 95% → подозрительно (бот?)
      12. **Повторяемость ввода:** слишком идентичные паттерны → подозрительно
    - Статусы: `VALID`, `INVALID`, `SUSPICIOUS`, `PENDING`
    - При `INVALID`: рейс помечается в `ts_race_results.validation = 'INVALID'`
    - При `SUSPICIOUS`: рейс помечается, уведомление админам
  - [ ] **Интеграция с RaceSession:**
    - Поле `telemetryReceived` (boolean) — получена ли телеметрия
    - Поле `telemetryValid` (boolean) — валидна ли
    - При финише: ожидание телеметрии до 30 секунд
    - Если не получена → рейс = INVALID
    - Если получена и валидна → рейс = VALID (засчитывается)
    - Если получена но невалидна → рейс = INVALID (не засчитывается)
  - [ ] **Интеграция с ts_race_results:**
    - Новая колонка: `validation_status VARCHAR(16) DEFAULT 'VALID'`
    - Невалидные рейсы:
      - НЕ попадают в лидерборд
      - НЕ засчитываются для рекордов
      - НЕ дают полные награды (только 20% «за участие»)
      - Видны в `/race results` с пометкой ❌
  - [ ] **Результаты валидации видимы игроку:**
    - При VALID: «✅ Рейс подтверждён»
    - При INVALID: «❌ Рейс не засчитан: [причина]»
    - При SUSPICIOUS: «⚠️ Рейс на проверке»

### 17.6 Базовая аналитика телеметрии (Плагин)
> **Суть:** Извлечение полезной статистики из телеметрии для игрока.

- [ ] **Функционал (плагин):**
  - [ ] `TelemetryAnalyzer.java` (новый класс):
    - Средняя скорость (км/ч)
    - Максимальная скорость (км/ч)
    - % времени в заносе (slipAngle > 5°)
    - % времени в воздухе
    - Кол-во столкновений (резкое падение скорости)
    - Распределение поверхностей (% асфальт, % грязь, etc.)
    - G-force статистика: среднее, макс. латеральное, макс. лонгитудинальное
  - [ ] Статистика сохраняется в `ts_telemetry_meta` (агрегированные поля)
  - [ ] Доступ через команды:
    - `/telemetry stats` — статистика последнего рейса
    - `/telemetry stats <игрок>` — статистика игрока (средние показатели)
    - `/telemetry compare <игрок>` — сравнение с другим игроком

### 17.7 Команды и конфигурация

- [ ] **Команды (мод — одиночная игра):**
  - [ ] `/telemetry` — статус записи (идёт / остановлена)
  - [ ] `/telemetry export` — экспорт последней записи в CSV
  - [ ] `/telemetry list` — список локальных файлов
- [ ] **Команды (плагин — сервер):**
  - [ ] `/telemetry stats [игрок]` — статистика
  - [ ] `/telemetry compare <игрок>` — сравнение
  - [ ] `/telemetry validate <raceId>` — ручная перевалидация (админ)
  - [ ] `/telemetry purge <дней>` — удалить файлы старше N дней (админ)
- [ ] **Конфигурация (config.yml):**
  ```yaml
  telemetry:
    enabled: true
    required_for_valid_race: true        # рейс без телеметрии = невалидный
    chunk_size_bytes: 16384              # 16 КБ
    transfer_timeout_seconds: 30
    max_retries: 3
    validation:
      enabled: true
      max_teleport_distance: 15.0        # блоков между тиками
      speed_tolerance: 1.2               # 20% допуск
      acceleration_tolerance: 1.3        # 30% допуск
      min_tick_coverage: 0.9             # минимум 90% тиков
      suspicious_full_throttle_percent: 0.95
    storage:
      directory: "telemetry"
      max_files_per_player: 100
      retention_days: 90
      client_retention_days: 30
      client_max_files: 500
  ```
- [ ] **Конфигурация (мод — ibrealistic.json):**
  ```json
  {
    "telemetry": {
      "saveLocally": true,
      "localRetentionDays": 30,
      "maxLocalFiles": 500,
      "autoResendPending": true,
      "exportFormat": "csv"
    }
  }
  ```
- [ ] **Permissions:**
  - [ ] `ts.telemetry.stats` — просмотр статистики (default: true)
  - [ ] `ts.telemetry.compare` — сравнение с другими (default: true)
  - [ ] `ts.telemetry.admin` — админ-команды (default: op)
- [ ] **Переводы:**
  - [ ] `lang/en_us.yml`: telemetry.* — ~25 ключей
  - [ ] Все языки + triton.yml + triton/timingsystem.json

### 17.8 Пакеты (сводка)
> **Новые пакеты на канале `ibrealistic:settings`:**
> - 80: `TELEMETRY_START` (client → server) — начало передачи
> - 81: `TELEMETRY_CHUNK` (client → server) — фрагмент данных
> - 82: `TELEMETRY_END` (client → server) — конец передачи
> - 83: `TELEMETRY_ACK` (server → client) — подтверждение чанка
> - 84: `TELEMETRY_RESULT` (server → client) — результат валидации
>
> **VERSION протокола:** 21 → **23** (пакеты 80–84)
> **realistic_version:** 1.1.0 → **1.2.0** (новая фича)

---

## ФАЗА 18: GHOST RACING / REPLAY 👻
> **Приоритет:** ВЫСОКИЙ — ghost racing — одна из самых популярных фич в гонках
> **Зависимости:** Фаза 17 (телеметрия — данные для replay)
> **Суть:** Игрок видит «призрак» своего лучшего заезда или мирового рекорда в реальном времени.

### 18.1 Хранение и управление ghost-данными (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `GhostManager.java` (новый класс):
    - При финише с VALID телеметрией:
      - Если время < personal best → сохранить как ghost для этого игрока+трека
      - Если время < world record → сохранить как ghost мирового рекорда
    - Хранение: `plugins/TimingSystem/ghosts/{track_id}/`
      - `{uuid}_pb.ghost` — personal best каждого игрока
      - `wr.ghost` — мировой рекорд
    - Формат ghost: упрощённая телеметрия (только позиция + yaw, ~24 байта/тик)
      ```
      GhostFrame:
      ─ posX, posY, posZ (float×3)   — позиция
      ─ yawAngle (float)             — поворот
      ─ steeringAngle (float)        — руль (для визуала)
      ─ speedKmh (float)             — скорость (для overlay)
      ```
    - Конвертация из телеметрии: `TelemetryToGhostConverter.java`
    - Максимум ghost-файлов: 1 PB + 1 WR на трек на игрока
  - [ ] Отправка ghost-данных клиенту:
    - При запросе ghost: сервер отправляет через фрагментированные пакеты
    - Пакеты:
      - `GHOST_DATA_START` (ID: 85, server → client): header (trackId, totalTicks, ghostType)
      - `GHOST_DATA_CHUNK` (ID: 86, server → client): чанк данных
      - `GHOST_DATA_END` (ID: 87, server → client): конец передачи
      - `GHOST_REQUEST` (ID: 88, client → server): запрос ghost (trackId, ghostType)
    - Ghost отправляется ДО старта рейса (во время countdown)

### 18.2 Отображение ghost на клиенте (Мод)

- [ ] **Функционал (мод):**
  - [ ] `GhostRenderer.java` (новый класс в `client/`):
    - Воспроизведение ghost как полупрозрачная лодка
    - Синхронизация: ghost стартует одновременно с игроком (при GO!)
    - Интерполяция позиции между тиками для плавности
    - Визуал:
      - Alpha = 0.4 (полупрозрачная)
      - Цвет: 🔴 красный = мировой рекорд, 🔵 синий = личный лучший
      - Колёса и руль рендерятся (используя steeringAngle из ghost)
    - Ghost виден ТОЛЬКО запросившему игроку (client-side entity)
    - При обгоне ghost: «+0.5s» / при отставании: «-0.3s» (дельта времени)
  - [ ] `GhostDataManager.java` (новый класс):
    - Приём ghost-данных от сервера
    - Хранение в памяти (не файл)
    - Очистка при смене трека / выходе
  - [ ] Overlay информация:
    - Расстояние до ghost (метры)
    - Дельта времени (± секунды, обновляется через чекпоинты)
    - Скорость ghost vs скорость игрока

### 18.3 Команды и конфигурация

- [ ] **Команды (плагин):**
  - [ ] `/ghost pb` — включить ghost личного лучшего
  - [ ] `/ghost wr` — включить ghost мирового рекорда
  - [ ] `/ghost player <имя>` — ghost лучшего заезда другого игрока
  - [ ] `/ghost off` — выключить ghost
  - [ ] `/ghost info` — информация о текущем ghost
- [ ] **Команды (мод — одиночная):**
  - [ ] `/ghost` — переключить отображение ghost
- [ ] **Конфигурация (config.yml):**
  ```yaml
  ghost:
    enabled: true
    auto_load_pb: true                   # автоматически загружать PB ghost при старте
    auto_load_wr: false                  # автоматически загружать WR ghost при старте
    max_ghost_ticks: 24000               # макс. длина ghost (20 минут)
    storage_directory: "ghosts"
    chunk_size_bytes: 16384
  ```
- [ ] **Переводы:**
  - [ ] `lang/en_us.yml`: ghost.* — ~15 ключей
  - [ ] Все языки + triton.yml + triton/timingsystem.json
- [ ] **Пакеты:**
  - 85: `GHOST_DATA_START` (server → client)
  - 86: `GHOST_DATA_CHUNK` (server → client)
  - 87: `GHOST_DATA_END` (server → client)
  - 88: `GHOST_REQUEST` (client → server)

---

## ФАЗА 19: АВТОМАТИЧЕСКИЕ ТУРНИРЫ 🏆
> **Приоритет:** ВЫСОКИЙ — создаёт соревновательный цикл и удерживает игроков
> **Зависимости:** Фаза 10 (система гонок), Фаза 8 (экономика), Фаза 17 (валидация)
> **Суть:** Еженедельные автоматические турниры. Игроки соревнуются за лучшее время.
> Победители получают награды. Рейтинговая система (ELO). Сезоны.

### 19.1 Менеджер турниров (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `TournamentManager.java` (новый класс):
    - Автоматический запуск турнира каждый понедельник в 00:00 UTC (настраиваемо)
    - Автоматический выбор треков: случайные N треков из пула (или фиксированный список)
    - Длительность: 7 дней (пн-вс), результаты считаются в воскресенье 23:59
    - Состояния: `SCHEDULED → ACTIVE → CALCULATING → FINISHED → ARCHIVED`
    - Автоматическое начисление наград победителям
    - Планировщик через BukkitScheduler (проверка каждые 5 минут)
  - [ ] `Tournament.java` (DTO):
    ```java
    @Getter @Setter
    public class Tournament {
        @Expose private int id;
        @Expose private String name;                    // "Турнир #42 — Sprint"
        @Expose private TournamentType type;            // SPRINT / RALLY / ENDURANCE
        @Expose private TournamentState state;
        @Expose private long startTimestamp;
        @Expose private long endTimestamp;
        @Expose private List<Integer> trackIds;
        @Expose private CarRestriction carRestriction;  // ALL / SYSTEM_ONLY / CUSTOM_ONLY
        @Expose private int seasonId;
        @Expose private Map<Integer, TournamentReward> rewards;  // позиция → награда
    }
    ```
  - [ ] `TournamentType.java` (enum):
    - **SPRINT** — лучшее время на одном треке
    - **RALLY** — суммарное время по серии треков (3-5 треков)
    - **ENDURANCE** — максимум кругов за фиксированное время
  - [ ] `TournamentState.java` (enum):
    - SCHEDULED, ACTIVE, CALCULATING, FINISHED, ARCHIVED, CANCELLED
  - [ ] `CarRestriction.java` (enum):
    - ALL, SYSTEM_ONLY, CUSTOM_ONLY
  - [ ] `TournamentReward.java`:
    - coins, xp, titleReward (String), exclusivePresetId (int, 0 = нет)

### 19.2 Участие и результаты (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `TournamentResult.java` (DTO):
    ```java
    @Getter @Setter
    public class TournamentResult {
        @Expose private int id;
        @Expose private int tournamentId;
        @Expose private UUID playerUuid;
        @Expose private Map<Integer, Long> trackTimes;  // trackId → bestTimeMs
        @Expose private long totalTimeMs;                // сумма лучших времён
        @Expose private int position;                    // итоговая позиция
        @Expose private boolean rewardClaimed;
    }
    ```
  - [ ] Участие автоматическое: любой VALID рейс на треке турнира во время турнира = участие
  - [ ] Для RALLY: учитывается лучшее время по КАЖДОМУ треку, сумма = итоговое время
  - [ ] Для SPRINT: учитывается лучшее время на треке
  - [ ] Для ENDURANCE: кол-во кругов за время
  - [ ] При завершении турнира (автоматически):
    1. Собрать все результаты
    2. Рассчитать позиции
    3. Начислить награды
    4. Обновить рейтинги (ELO)
    5. Broadcast результатов
  - [ ] Минимум N участников (настраиваемо, default: 3), иначе отмена

### 19.3 Рейтинговая система ELO (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `RatingManager.java` (новый класс):
    - Алгоритм: Glicko-2 (улучшенный ELO с учётом волатильности)
    - Начальный рейтинг: 1000 (настраиваемо)
    - K-factor: 32 (настраиваемо)
    - Рейтинг обновляется после каждого турнира
    - Учёт позиции в турнире (попарные сравнения)
  - [ ] Таблица `ts_player_rating`:
    ```sql
    CREATE TABLE ts_player_rating (
      uuid VARCHAR(36) PRIMARY KEY,
      rating INT DEFAULT 1000,
      deviation INT DEFAULT 350,          -- неопределённость (Glicko-2)
      volatility FLOAT DEFAULT 0.06,      -- волатильность (Glicko-2)
      games_played INT DEFAULT 0,
      peak_rating INT DEFAULT 1000,
      season_id INT DEFAULT 1,
      updated_at TIMESTAMP
    );
    ```
  - [ ] Ранги по рейтингу (визуальные):
    - 🥉 Bronze: 0–999
    - 🥈 Silver: 1000–1299
    - 🥇 Gold: 1300–1599
    - 💎 Diamond: 1600–1899
    - 👑 Champion: 1900+

### 19.4 Сезонная система (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `SeasonManager.java` (новый класс):
    - Сезон = 3 месяца (настраиваемо)
    - Очки за каждый турнир (основаны на позиции):
      - 1 место: 25 очков, 2 место: 18, 3 место: 15, 4: 12, 5: 10, 6: 8, 7: 6, 8: 4, 9: 2, 10: 1
    - Итоговые сезонные награды по очкам
    - Soft reset рейтинга при новом сезоне: `newRating = (rating - 1000) × 0.5 + 1000`
  - [ ] Таблица `ts_seasons`:
    ```sql
    CREATE TABLE ts_seasons (
      id INT AUTO_INCREMENT PRIMARY KEY,
      name VARCHAR(64),
      start_date TIMESTAMP,
      end_date TIMESTAMP,
      state VARCHAR(16) DEFAULT 'ACTIVE'  -- ACTIVE / FINISHED
    );
    ```
  - [ ] Таблица `ts_season_points`:
    ```sql
    CREATE TABLE ts_season_points (
      uuid VARCHAR(36),
      season_id INT,
      total_points INT DEFAULT 0,
      tournaments_played INT DEFAULT 0,
      best_position INT DEFAULT 0,
      PRIMARY KEY (uuid, season_id)
    );
    ```

### 19.5 Таблицы БД турниров

- [ ] **Миграция (Version23):**
  - [ ] `ts_tournaments`:
    ```sql
    CREATE TABLE ts_tournaments (
      id INT AUTO_INCREMENT PRIMARY KEY,
      name VARCHAR(128),
      type VARCHAR(16),
      state VARCHAR(16) DEFAULT 'SCHEDULED',
      start_timestamp BIGINT,
      end_timestamp BIGINT,
      track_ids TEXT,                      -- JSON array [1, 5, 12]
      car_restriction VARCHAR(16) DEFAULT 'ALL',
      season_id INT,
      rewards TEXT,                        -- JSON {1: {coins: 500, xp: 1000}, ...}
      min_players INT DEFAULT 3,
      created_at TIMESTAMP
    );
    ```
  - [ ] `ts_tournament_results`:
    ```sql
    CREATE TABLE ts_tournament_results (
      id INT AUTO_INCREMENT PRIMARY KEY,
      tournament_id INT,
      uuid VARCHAR(36),
      track_times TEXT,                    -- JSON {trackId: bestTimeMs, ...}
      total_time_ms BIGINT,
      position INT DEFAULT 0,
      reward_claimed BOOLEAN DEFAULT FALSE,
      INDEX idx_tournament (tournament_id),
      INDEX idx_uuid (uuid)
    );
    ```
  - [ ] `ts_player_rating` + `ts_seasons` + `ts_season_points` (см. выше)

### 19.6 Команды

- [ ] **Команды:**
  - [ ] `/tournament` — текущий активный турнир (треки, время, результаты)
  - [ ] `/tournament results` — результаты текущего/последнего турнира
  - [ ] `/tournament top` — топ участников
  - [ ] `/tournament history` — история турниров
  - [ ] `/tournament rating` — рейтинг ELO (свой + топ)
  - [ ] `/tournament season` — информация о текущем сезоне
  - [ ] `/tournament admin create <type> [tracks...]` — ручное создание
  - [ ] `/tournament admin cancel` — отмена текущего турнира
  - [ ] `/tournament admin rewards <pos> <coins> <xp>` — настройка наград
  - [ ] `/tournament admin season start` — начать новый сезон
- [ ] **Permissions:**
  - [ ] `ts.tournament.view` — просмотр турниров (default: true)
  - [ ] `ts.tournament.admin` — админ-команды (default: op)
- [ ] **Конфигурация (config.yml):**
  ```yaml
  tournaments:
    enabled: true
    auto_start_day: "MONDAY"
    auto_start_hour: 0
    duration_days: 7
    tracks_count: 3
    track_pool_tag: ""                    # пустой = все треки
    car_restriction: "ALL"
    min_players: 3
    rewards:
      1: { coins: 500, xp: 1000 }
      2: { coins: 300, xp: 600 }
      3: { coins: 200, xp: 400 }
      participation: { coins: 50, xp: 100 }
    season:
      duration_months: 3
      reset_rating: true
      season_rewards:
        1: { coins: 2000, xp: 5000 }
        2: { coins: 1000, xp: 3000 }
        3: { coins: 500, xp: 1500 }
    rating:
      initial: 1000
      k_factor: 32
  ```
- [ ] **Уведомления:**
  - [ ] При старте турнира: broadcast
  - [ ] Ежедневное напоминание (actionbar)
  - [ ] При завершении: broadcast с топ-3
  - [ ] При получении награды
- [ ] **Переводы:** tournament.*, season.*, rating.* — ~40 ключей во всех lang файлах + triton

---

## ФАЗА 20: КОМАНДНЫЕ ГОНКИ С ПИТСТОПАМИ 🏎️
> **Приоритет:** ВЫСОКИЙ — уникальная социальная фича (F1-стиль)
> **Зависимости:** Фаза 13 (система поломок/износа), Фаза 10 (мультиплеер-режим)
> **Суть:** Настоящие командные гонки: 1 пилот + 2-3 механика.
> Механики в пит-зоне кликают хотбар-предметы для обслуживания.

### 20.1 Система команд (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `Team.java` (DTO):
    ```java
    @Getter @Setter
    public class Team {
        @Expose private int id;
        @Expose private String name;
        @Expose private UUID ownerUuid;
        @Expose private List<TeamMember> members;
        @Expose private int maxMembers = 4;            // 1 пилот + 3 механика
        @Expose private long createdAt;
    }
    ```
  - [ ] `TeamMember.java`:
    ```java
    @Getter @Setter
    public class TeamMember {
        @Expose private UUID uuid;
        @Expose private TeamRole role;                  // PILOT / MECHANIC
        @Expose private long joinedAt;
    }
    ```
  - [ ] `TeamRole.java` (enum): PILOT, MECHANIC
  - [ ] `TeamManager.java`:
    - Создание/удаление команд
    - Приглашение/исключение участников
    - Назначение ролей
    - Максимум 1 команда на игрока
    - Таблица `ts_teams` + `ts_team_members`
  - [ ] Таблицы БД (Version24):
    ```sql
    CREATE TABLE ts_teams (
      id INT AUTO_INCREMENT PRIMARY KEY,
      name VARCHAR(64) UNIQUE,
      owner_uuid VARCHAR(36),
      max_members INT DEFAULT 4,
      created_at TIMESTAMP
    );

    CREATE TABLE ts_team_members (
      team_id INT,
      uuid VARCHAR(36),
      role VARCHAR(16) DEFAULT 'MECHANIC',
      joined_at TIMESTAMP,
      PRIMARY KEY (team_id, uuid),
      INDEX idx_uuid (uuid)
    );
    ```

### 20.2 Механика питстопов (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `PitStopManager.java` (новый класс):
    - Когда пилот заезжает в зону SERVICEPARK (существующий RegionType):
      1. Пилот замораживается (walkSpeed = 0)
      2. Механикам выдаётся специальный хотбар
      3. Таймер пит-стопа запускается
    - Хотбар-предметы для механиков:
      - 🔧 **Шины** (IRON_HOE, слот 0): клик = замена 1 шины. Нужно 4 клика = все 4 шины
        - Каждый клик: анимация + звук гайковёрта
        - Визуальный прогресс: lore обновляется (1/4, 2/4, 3/4, 4/4)
      - ⛽ **Заправка** (BLAZE_ROD, слот 1): удержание ПКМ = заправка
        - Прогресс: BossBar (0% → 100%), время: 5 секунд (настраиваемо)
        - Не реализовано как механика, а как восстановление двигателя (снижает engineTemp)
      - 🔩 **Ремонт кузова** (ANVIL, слот 2): быстрые клики = ремонт повреждений
        - Нужно 6 кликов (настраиваемо)
        - Снижает bodyDamage на 0.15 за клик
      - ✅ **Готово** (LIME_DYE, слот 3): отпускает пилота
        - Активна ТОЛЬКО когда все задачи выполнены
        - Если нажата раньше → предупреждение
    - Таймер пит-стопа:
      - BossBar показывает прогресс всех задач
      - Минимальное время: 8 секунд (если всё идеально)
      - За каждую секунду задержки → +2 секунды штрафа к общему времени
    - Пит-стоп завершается:
      - Когда механик нажимает ✅ (все задачи выполнены)
      - ИЛИ таймаут (30 секунд по умолчанию) → автовыброс с неполным ремонтом
  - [ ] Восстановление при пит-стопе:
    - tireWear: сброс до 0.0 (полная замена шин)
    - engineTemp: снижение до 0.1 (охлаждение)
    - bodyDamage: снижение на (0.15 × кол-во кликов ремонта)

### 20.3 Командная гонка (Плагин)

- [ ] **Функционал (плагин):**
  - [ ] `TeamRaceSession.java` (extends RaceSession):
    - Ссылка на Team
    - Список PitStopSession (история пит-стопов)
    - Суммарное время = время гонки + суммарное время пит-стопов + штрафы
  - [ ] Командный лидерборд:
    - Таблица `ts_team_race_results`:
      ```sql
      CREATE TABLE ts_team_race_results (
        id INT AUTO_INCREMENT PRIMARY KEY,
        team_id INT,
        track_id INT,
        pilot_uuid VARCHAR(36),
        race_time_ms BIGINT,
        pit_time_ms BIGINT,
        penalty_ms BIGINT DEFAULT 0,
        total_time_ms BIGINT,
        created_at TIMESTAMP,
        INDEX idx_team (team_id),
        INDEX idx_track (track_id)
      );
      ```
  - [ ] Статистика пит-стопов:
    - Лучший пит-стоп (ms)
    - Средний пит-стоп
    - Кол-во пит-стопов за гонку

### 20.4 Команды

- [ ] **Команды:**
  - [ ] `/team create <название>` — создать команду
  - [ ] `/team invite <игрок>` — пригласить
  - [ ] `/team accept` — принять приглашение
  - [ ] `/team decline` — отклонить
  - [ ] `/team kick <игрок>` — исключить
  - [ ] `/team role <игрок> <pilot|mechanic>` — назначить роль
  - [ ] `/team leave` — покинуть команду
  - [ ] `/team disband` — расформировать (только владелец)
  - [ ] `/team info` — информация о команде
  - [ ] `/team race <трек> [кругов] [обязательных_пит]` — начать командную гонку
  - [ ] `/team results [трек]` — результаты командных гонок
- [ ] **Permissions:**
  - [ ] `ts.team.create` — создание команды (default: true)
  - [ ] `ts.team.race` — запуск командной гонки (default: true)
  - [ ] `ts.team.admin` — админ-команды (default: op)
- [ ] **Конфигурация (config.yml):**
  ```yaml
  team_race:
    enabled: true
    min_mechanics: 1
    max_mechanics: 3
    pitstop:
      tire_clicks: 4
      refuel_time_seconds: 5
      repair_clicks: 6
      min_pitstop_seconds: 8
      timeout_seconds: 30
      penalty_per_extra_second: 2
  ```
- [ ] **Переводы:** team.*, pitstop.* — ~30 ключей во всех lang файлах + triton

---

## ФАЗА 16: ДОПОЛНИТЕЛЬНЫЕ ИДЕИ (БЭКЛОГ) 💡

> Все предложения из code review, CHANGES файлов и обсуждений — организованы по категориям.
> Приоритет внутри категорий: от наиболее полезных к наименее.

### ГЕЙМПЛЕЙ — ОСНОВНОЕ

#### ~~16.1 Автоматические еженедельные турниры 🏆~~ → **ФАЗА 19**
> Перенесено в полноценную Фазу 19. Подробный план: см. выше.

#### 16.2 Система достижений 🏅
> **Приоритет:** Средний — добавляет долгосрочную мотивацию и цели

- [ ] **Функционал (плагин):**
  - [ ] `AchievementManager.java`:
    - Категории: Гонки, Экономика, Кастомизация, Социальные, Мастерство
    - Проверка прогресса при завершении гонки, покупке, уровне
    - Уведомление при получении: title + sound + частицы
  - [ ] Примеры достижений:
    - 🏁 **Road Warrior** — пройти 100 треков
    - ⏱️ **Record Breaker** — побить 50 рекордов
    - 💰 **Millionaire** — заработать 10000 монет
    - 🏎️ **Collector** — купить все пресеты одной категории
    - 🎯 **Perfect Run** — проехать трек без столкновений
    - 🔥 **Streak Master** — 7 дней подряд выполнять все дейли
    - 🏆 **Champion** — выиграть турнир
  - [ ] Награды за достижения:
    - Множители XP/монет (1.1x, 1.2x — стакаются)
    - Эксклюзивные пресеты компонентов (только через достижения)
    - Титулы (отображаются в чате и профиле)
  - [ ] Таблицы БД:
    - `ts_achievements` (id, name, category, description, requirement_type, requirement_value, reward_type, reward_value)
    - `ts_player_achievements` (uuid, achievement_id, progress, completed, completed_at)
- [ ] **Команды:**
  - [ ] `/achievements` — список достижений с прогрессом
  - [ ] `/achievements <категория>` — фильтр по категории
  - [ ] `/achievements admin add <игрок> <id>` — ручная выдача
- [ ] **Конфигурация:**
  ```yaml
  achievements:
    enabled: true
    notification_sound: "entity.player.levelup"
    show_title: true
    multiplier_stacking: true       # множители стакаются
  ```
- [ ] **Переводы:** achievements.* сообщения во всех lang файлах + triton

#### ~~16.3 Командные гонки с питстопами (F1-стиль) 🏎️~~ → **ФАЗА 20**
> Перенесено в полноценную Фазу 20. Подробный план: см. выше.

#### 16.4 Система ставок 🎰
> **Приоритет:** Средний — добавляет азарт и интерес зрителей

- [ ] **Функционал (плагин):**
  - [ ] `BettingManager.java`:
    - Ставки открываются при старте мультиплеерной гонки (countdown)
    - Ставки закрываются при GO (нельзя ставить после старта)
    - Автоматические коэффициенты на основе ELO рейтинга (из турниров 16.1)
    - Формула коэффициентов: `odds = totalPool / playerPool × (1 - commission)`
    - Комиссия системы: 5% (настраиваемо)
    - Автоматическая выплата при завершении гонки
  - [ ] Типы ставок:
    - **Победитель** — кто финиширует первым
    - **Топ-3** — попадёт ли игрок в тройку
    - **Time Challenge** — побьёт ли игрок время X
    - **Head-to-Head** — кто из двоих финиширует первым
  - [ ] Антифрод:
    - Нельзя ставить на свою гонку
    - Минимум 3 участника для открытия ставок
    - Максимальная ставка: 20% от баланса игрока
    - Лимит ставок в день: 5 (настраиваемо)
  - [ ] Таблицы БД:
    - `ts_bets` (id, bettor_uuid, race_id, bet_type, target_uuid, amount, odds, payout, status)
    - `ts_bet_history` (id, bettor_uuid, amount, payout, profit, timestamp)
- [ ] **Команды:**
  - [ ] `/bet <игрок> <сумма>` — ставка на победу игрока
  - [ ] `/bet top3 <игрок> <сумма>` — ставка на попадание в топ-3
  - [ ] `/bet time <игрок> <время> <сумма>` — ставка на время
  - [ ] `/bet h2h <игрок1> <игрок2> <сумма>` — head-to-head
  - [ ] `/bet odds` — текущие коэффициенты
  - [ ] `/bet history` — история ставок
  - [ ] `/bet admin commission <процент>` — комиссия
- [ ] **Конфигурация:**
  ```yaml
  betting:
    enabled: true
    commission: 0.05              # 5% комиссия
    min_bet: 10                   # минимальная ставка
    max_bet_percent: 0.2          # 20% от баланса
    daily_bet_limit: 5            # макс. ставок в день
    min_participants: 3           # мин. участников для ставок
    allowed_bet_types:
      - WINNER
      - TOP3
      - TIME_CHALLENGE
      - HEAD_TO_HEAD
  ```
- [ ] **Переводы:** bet.* сообщения во всех lang файлах + triton

#### 16.5 Улучшение ежедневных заданий 📋
> **Приоритет:** Средний — текущая система (Фаза 8.4) базовая, нужно расширить

- [ ] **Функционал (плагин):**
  - [ ] Новые типы заданий:
    - 🎯 **Clean Run** — проехать трек без единого столкновения
    - ⏱️ **Close Call** — завершить гонку с ≤2 сек разницы от мирового рекорда
    - 🏎️ **Car Challenge** — проехать трек на определённой машине/пресете
    - 🛣️ **Distance Runner** — проехать суммарно 50 км за день
    - 🏆 **Winner** — выиграть мультиплеерную гонку
    - 🔧 **Pit Crew** — успешно провести пит-стоп в командной гонке (зависит от 16.3)
    - 🌧️ **Weather Master** — завершить гонку в дождь/снег без сходов
    - 📈 **Improver** — побить своё личное лучшее время на любом треке
  - [ ] Недельные задания (Weekly Challenges):
    - Сложнее дейли, больше награда (3x)
    - Примеры: «Завершить 20 гонок за неделю», «Побить 5 рекордов»
    - Отдельный пул заданий, не пересекается с дейли
  - [ ] Streak-бонус:
    - +10% к наградам за каждый последовательный день выполнения ВСЕХ заданий
    - Максимум: +70% (7 дней подряд)
    - При пропуске: streak сбрасывается
    - Визуальный индикатор streak: 🔥×1, 🔥×2, ..., 🔥×7
  - [ ] `DailyChallengeType.java` — enum с новыми типами (CLEAN_RUN, CLOSE_CALL, CAR_CHALLENGE, etc.)
  - [ ] `WeeklyChallengeManager.java` — управление недельными заданиями
  - [ ] Таблица `ts_player_streak` (uuid, current_streak, max_streak, last_completion_date)
- [ ] **Команды:**
  - [ ] `/daily` — текущие задания (дейли + недельные)
  - [ ] `/daily streak` — информация о streak-бонусе
  - [ ] `/weekly` — только недельные задания
- [ ] **Конфигурация:**
  ```yaml
  daily_challenges:
    count: 3
    streak_bonus_percent: 10       # +10% за каждый день
    streak_max_days: 7
    weekly:
      enabled: true
      count: 2
      reward_multiplier: 3.0
  ```
- [ ] **Переводы:** daily.streak.*, weekly.* сообщения

#### 16.6 Настраиваемые множители наград трека
> **Из Phase 8.5:** индивидуальные множители через TrackTag

- [ ] **Функционал (плагин):**
  - [ ] `TrackRewardMultiplier.java`:
    - Каждый трек может иметь множитель монет и XP
    - Хранение через `TrackTag` (уже существует)
    - Множитель применяется при начислении награды (в `RaceRewardManager`)
    - По умолчанию: 1.0x (без изменений)
    - Допустимый диапазон: 0.0–5.0
  - [ ] Использование: сложные треки → больший множитель, лёгкие → меньший
- [ ] **Команды:**
  - [ ] `/te rewardmultiplier coins <трек> <множитель>` — множитель монет
  - [ ] `/te rewardmultiplier xp <трек> <множитель>` — множитель XP
  - [ ] `/te rewardmultiplier info <трек>` — текущие множители
- [ ] **Переводы:** reward.multiplier.* сообщения

### ТРЕКИНГ И АНАЛИТИКА

#### ~~16.7 Система трекинга через мод → сервер 📊~~ → **ФАЗА 17**
> Перенесено в полноценную Фазу 17. Подробный план: см. выше.

#### ~~16.8 Replay система / Ghost racing 👻~~ → **ФАЗА 18**
> Перенесено в полноценную Фазу 18. Подробный план: см. выше.

#### 16.9 Телеметрия (визуализация) 📊
> **Из code review:** визуализация данных из трекинга
> **Зависимость:** Фаза 17 (телеметрия — источник данных)

- [ ] **Функционал (мод):**
  - [ ] `TelemetryOverlay.java`:
    - HUD overlay с графиками в реальном времени:
      - Скорость (км/ч) — линейный график
      - Slip angle (°) — 4 колеса, цвет по уровню
      - G-force (g) — круговая диаграмма (lat/long)
      - Throttle/Brake input — бары
    - Переключение видимости: keybind (F9 или настраиваемо)
    - Размер/позиция overlay настраиваемы
  - [ ] Экспорт данных:
    - Клавиша для старта/остановки записи
    - Экспорт в CSV: timestamp, x, y, z, vx, vy, yaw, steer, throttle, brake, surface
    - Экспорт в JSON: структурированный формат для веб-анализа
  - [ ] Мини-карта скорости на трассе (heatmap)
- [ ] **Функционал (плагин — опционально):**
  - [ ] Встроенный HTTP-сервер (`TelemetryWebServer.java`):
    - Эндпоинт: `/api/telemetry/{uuid}/{race_id}`
    - Веб-страница с интерактивными графиками (Chart.js)
    - Порт настраивается в config.yml
- [ ] **Конфигурация:**
  ```yaml
  telemetry:
    overlay_keybind: "F9"
    export_directory: "telemetry_export"
    web_server:
      enabled: false
      port: 8080
  ```
- [ ] **Переводы:** telemetry.* сообщения

### ФИЗИКА (РАСШИРЕНИЕ)

#### 16.10 Система контроля тяги (Traction Control)
> **Из code review:** ограничение пробуксовки при ускорении

- [ ] **Функционал (мод):**
  - [ ] В `FourWheelPhysicsEngine.java`:
    - Если slip ratio любого ведущего колеса > `tcsThreshold` → уменьшить engineForce
    - Алгоритм: `effectiveForce = engineForce × (1.0 - tcsReduction × excessSlip)`
    - `tcsThreshold` = 0.15 (настраиваемо через пресет)
    - `tcsReduction` = 0.8 (насколько агрессивно TCS режет мощность)
  - [ ] Визуальный индикатор: мигающая иконка «TCS» в HUD при срабатывании
  - [ ] Настройка: вкл/выкл через команду или пресет
- [ ] **Функционал (плагин):**
  - [ ] Пакет `SET_TRACTION_CONTROL` (server → client): вкл/выкл + параметры
  - [ ] Поле в `CustomBoatUtilsMode.java`: `tractionControl` (boolean + threshold + reduction)
  - [ ] Команда в `CommandBoatUtilsModeEdit.java`: `tractioncontrol <on|off> [threshold] [reduction]`
- [ ] **Конфигурация (через CustomBoatUtilsMode):**
  - tcsEnabled: boolean
  - tcsThreshold: 0.15
  - tcsReduction: 0.8
- [ ] **Переводы:** tcs.* сообщения

#### 16.11 Настройка передаточных чисел (Gear Ratios) ⚙️
> **Из code review:** конфигурация передач для разных скоростных режимов

- [ ] **Функционал (мод — physics/):**
  - [ ] `GearboxConfig.java`:
    - Массив передаточных чисел: `float[] gearRatios = {3.5, 2.5, 1.8, 1.3, 1.0, 0.8}`
    - Главная передача (final drive): `float finalDrive = 4.0`
    - Текущая передача: `int currentGear`
    - Обороты двигателя: `rpm = wheelSpeed × gearRatios[gear] × finalDrive`
  - [ ] В `FourWheelPhysicsEngine.java`:
    - Автоматическое переключение: вверх при rpm > `upshiftRpm`, вниз при rpm < `downshiftRpm`
    - Задержка переключения: 0.3 сек (потеря мощности на время переключения)
    - Влияние на ускорение: короткие передачи = быстрый разгон, длинные = высокая максималка
  - [ ] Пресеты:
    - **Short** — ускорение (более частое переключение, низкая максималка)
    - **Long** — максимальная скорость (редкое переключение)
    - **Balanced** — средний вариант
    - **Rally** — укороченные, оптимизированные для разгона из поворотов
- [ ] **Функционал (плагин):**
  - [ ] Пакет `SET_GEAR_RATIOS` (server → client): массив ratios + finalDrive
  - [ ] Пресеты gearbox как часть кастомизации (Фаза 9)
- [ ] **Команды:** `/gearbox <preset|custom> [ratios...]`
- [ ] **Переводы:** gearbox.* сообщения

### ПРОСМОТР

#### 16.12 Спектатор-режим 📺
> **Приоритет:** Средний — для стримов и турниров

- [ ] **Функционал (плагин + мод):**
  - [ ] `SpectatorManager.java` (плагин):
    - Режим спектатора привязан к конкретной гонке
    - Список участников гонки → выбор кого смотреть
    - Данные о позиции/скорости всех гонщиков отправляются спектатору
  - [ ] `SpectatorCamera.java` (мод):
    - **Follow Cam** — камера следит за выбранным гонщиком (3rd person сзади)
    - **Free Cam** — свободная камера (WASD для перемещения)
    - **Switch** — переключение между гонщиками (Tab или стрелки)
    - **Bird's Eye** — вид сверху на трассу
  - [ ] Overlay для спектатора:
    - Имя гонщика, скорость, позиция в рейсе
    - Время текущего круга / разница с лидером
    - Миникарта: позиции всех гонщиков (цветные точки)
  - [ ] Пакеты: `SPECTATOR_DATA` (server → client): позиции всех участников каждый тик
- [ ] **Команды:**
  - [ ] `/spectate <гонка>` — войти в спектатор-режим
  - [ ] `/spectate follow <игрок>` — следить за конкретным гонщиком
  - [ ] `/spectate free` — свободная камера
  - [ ] `/spectate leave` — выйти
- [ ] **Конфигурация:**
  ```yaml
  spectator:
    enabled: true
    max_spectators_per_race: 10
    minimap_enabled: true
    update_interval_ticks: 2      # частота обновления позиций
  ```
- [ ] **Переводы:** spectator.* сообщения

### ГОЛОСОВЫЕ PACE NOTES 🎤

#### 16.13 Голосовая система pace notes (Text-to-Speech)
> **Приоритет:** Средний — уникальная фича, реализация позже
> **Суть:** Автогенерация голосовых записей pace notes через TTS API.

- [ ] **Функционал (плагин):**
  - [ ] `PaceNoteVoiceManager.java`:
    - Интеграция с TTS API (Google Cloud TTS / Amazon Polly / Azure / OpenAI TTS)
    - При сохранении/редактировании pace note → отправка текста в TTS API
    - Генерация OGG Vorbis файлов (формат Minecraft resource pack)
    - Кэширование: одна фраза = один файл, повторная генерация только при изменении
    - Поддержка языков: русский (ru-RU) и английский (en-US) для начала
    - Выбор голоса: мужской/женский, тембр настраивается
    - Хранение: `plugins/TimingSystem/pacenotes/{track_id}/{lang}/{note_id}.ogg`
  - [ ] Автогенерация по геометрии трека:
    - Анализ чекпоинтов: определение углов между соседними точками
    - Классификация поворотов (1-6 по раллийной шкале: 1=шпилька, 6=лёгкий)
    - Генерация стандартных фраз:
      ```
      EN: "Left 3, 50, right 5 long, caution jump"
      RU: "Левый 3, 50, правый 5 длинный, осторожно трамплин"
      ```
    - Учёт расстояний для таймингов между фразами
- [ ] **Функционал (мод):**
  - [ ] Воспроизведение: resource pack с OGG файлами (серверный RP)
  - [ ] При подходе к чекпоинту → сервер отправляет sound event
  - [ ] Настройка: расстояние предупреждения (50–150м до точки)
  - [ ] Громкость: категория VOICE (отдельная регулировка в MC)
  - [ ] Очередь: фразы последовательно, без перекрытия
  - [ ] Пакет `PACE_NOTE_AUDIO` (server → client)
- [ ] **Команды:**
  - [ ] `/pacenotes generate <трек> [язык]` — генерация голоса для заметок
  - [ ] `/pacenotes autogenerate <трек> [язык]` — автогенерация по геометрии
  - [ ] `/pacenotes preview <трек> <note_id>` — предпрослушка
  - [ ] `/pacenotes language <ru|en>` — выбор языка
  - [ ] `/pacenotes enable|disable` — вкл/выкл для игрока
- [ ] **Конфигурация:**
  ```yaml
  pacenotes:
    tts_provider: "google"
    tts_api_key: ""
    default_language: "en"
    default_voice: "male"
    warning_distance: 100
    cache_directory: "pacenotes"
    auto_generate: false
    supported_languages: [en, ru]
  ```
- [ ] **Переводы:** pacenotes.* сообщения

### ИНТЕГРАЦИИ

#### 16.14 Интеграция с Discord 💬
> **Приоритет:** Средний — расширяет присутствие сервера

- [ ] **Функционал (плагин):**
  - [ ] `DiscordIntegration.java`:
    - Webhook: автоматическая отправка результатов гонок в Discord канал
    - Формат: embed с именем, треком, временем, позицией, наградой
    - Автообновляемый лидерборд: сообщение в Discord, обновляется каждый час
    - Уведомления о турнирах: начало, результаты, сезонный итог
    - Интеграция через DiscordSRV (если установлен) или собственный HTTP client
  - [ ] Привязка аккаунтов:
    - `/discord link <код>` — привязка Discord → MC
    - Роли в Discord по уровню/рейтингу (через DiscordSRV)
- [ ] **Конфигурация:**
  ```yaml
  discord:
    enabled: false
    webhook_url: ""
    leaderboard_channel_id: ""
    update_interval_minutes: 60
    embed_color: "#FF6B35"
    notifications:
      race_results: true
      tournament_start: true
      tournament_results: true
      new_record: true
  ```
- [ ] **Переводы:** discord.* сообщения

#### 16.15 PlaceholderAPI расширения
> **Приоритет:** Низкий — интеграция со скорбордами и табом

- [ ] **Функционал (плагин):**
  - [ ] `IBRealisticExpansion.java` extends PlaceholderExpansion:
    - `%ts_realistic_level%` — уровень игрока
    - `%ts_realistic_xp%` — текущий XP
    - `%ts_realistic_xp_next%` — XP до следующего уровня
    - `%ts_realistic_coins%` — баланс монет
    - `%ts_realistic_car%` — текущая машина (VehicleType name)
    - `%ts_realistic_total_races%` — общее количество гонок
    - `%ts_realistic_total_wins%` — общее количество побед
    - `%ts_realistic_best_time_{track}%` — лучшее время на треке (форматированное)
    - `%ts_realistic_rank%` — рейтинг ELO
    - `%ts_realistic_tournament_pos%` — позиция в текущем турнире
    - `%ts_realistic_streak%` — текущий streak дейли
    - `%ts_realistic_season_points%` — очки в сезоне
  - [ ] Зависимость: PlaceholderAPI (soft dependency в plugin.yml)

### ТЕХНИЧЕСКИЕ УЛУЧШЕНИЯ

#### 16.16 Оптимизация производительности ⚡
> **Из code review:** потенциальные узкие места

- [ ] **Функционал (мод — physics/):**
  - [ ] Кэширование surface detection:
    - Хранить последний блок + surface type → не пересчитывать если позиция не изменилась
    - Инвалидация: каждые 5 тиков или при перемещении > 0.5 блока
  - [ ] Оптимизация подшагов:
    - Адаптивный substep count: меньше подшагов при низкой скорости
    - При speed < 5 м/с → 2 substeps, при > 20 м/с → 8 substeps
  - [ ] Предрасчёт тригонометрии:
    - LUT (lookup table) для sin/cos при расчёте сил
    - Точность ±0.001 достаточна для игровой физики
- [ ] **Функционал (плагин):**
  - [ ] Оптимизация SQL запросов:
    - Connection pooling (HikariCP уже используется)
    - Batch insert для телеметрии
    - Индексы на часто запрашиваемые колонки (uuid, track_id, timestamp)
  - [ ] Профилирование на серверах с 50+ игроков
  - [ ] Асинхронные операции для некритичных задач (статистика, достижения)

#### 16.17 Улучшенные колёсные модели 🎨
> **Из CHANGES_steering_return_visual_roll_wheels.md:** текущие колёса — простые кубоиды

- [ ] **Функционал (мод — client/):**
  - [ ] `DetailedWheelRenderer.java`:
    - Замена кубоидов на детальные 3D модели (цилиндр + спицы + протектор)
    - Текстуры зависят от пресета шин (Soft=красная полоса, Hard=белая, Rain=синяя)
    - Анимация деформации при нагрузке (лёгкое сплющивание по вертикали)
    - Следы от покрышек на земле (particle trail с цветом от поверхности)
  - [ ] Переключение: high/low quality через настройки мода
  - [ ] Resource pack: модели + текстуры для каждого пресета

#### 16.18 Поддержка рулей и педалей (через внешний мод) 🎮
> **Приоритет:** САМЫЙ НИЗКИЙ — реализация через внешний мод-аддон
> **Концепция:** Отдельный мод (не IBRealistic) читает ввод с руля/педалей через GLFW.
> IBRealistic предоставляет API для получения аналоговых значений и ОТКЛЮЧАЕТ встроенную
> задержку/сглаживание руля, т.к. у физического руля задержка уже естественная.

- [ ] **API для внешнего мода (IBRealistic):**
  - [ ] Пакет `SET_ANALOG_INPUT_MODE` (server → client): переключение в аналоговый режим
  - [ ] При аналоговом режиме:
    - Отключается steeringReturnRate (руль не возвращается программно)
    - Отключается плавное нарастание/затухание steeringInput
    - steeringAngle = прямое значение от контроллера (0.0–1.0 × maxSteeringAngle)
    - throttle/brake = прямые значения (0.0–1.0)
  - [ ] Интерфейс: `IBRealistic.setAnalogSteering(float)`, `IBRealistic.setAnalogThrottle(float)`
- [ ] **Внешний мод-аддон (отдельный проект):**
  - [ ] Чтение аналоговых осей через GLFW (`glfwGetJoystickAxes`)
  - [ ] Маппинг осей: руль, газ, тормоз, ручник
  - [ ] Поддержка мёртвых зон (deadzone)
  - [ ] Калибровка: min/max/center
  - [ ] GUI настроек: назначение осей, калибровка, пресеты (G27/G29/G920, Thrustmaster)
- [ ] **Force Feedback (в внешнем моде):**
  - [ ] Данные для FFB предоставляет IBRealistic: lateralForce, gripLevel, speed, collisionImpact
  - [ ] Внешний мод читает данные и отправляет FFB-команды через GLFW/SDL

---

## ПРИНЦИПЫ РАЗРАБОТКИ

### Порядок реализации в каждой фазе
```
1. Бэкенд (данные, БД, менеджеры)
2. Пакеты (клиент-сервер, если нужно)
3. Команды (для тестирования без GUI)
4. Переводы (все языковые файлы)
5. Тестирование (все MC версии)
6. GUI (ТОЛЬКО после работающего бэкенда)
7. Визуальные эффекты (ПОСЛЕДНИМИ)
```

### Переводы — в каждом шаге
Каждый шаг каждой фазы ДОЛЖЕН включать:
1. Добавление ключей в `lang/en_us.yml` (основной)
2. Добавление обёрток в `lang/triton.yml` (`[lang]ts.key[/lang]`)
3. Placeholder-переводы в остальных языковых файлах (`de_de.yml`, `id_id.yml`, `zh_cn.yml`, `pl_pl.yml`, `nl_nl.yml`, `es_es.yml`, `pt_br.yml`, `fr_fr.yml`)
4. Добавление записей в `triton/timingsystem.json` (en_GB, ru_RU, de_DE, pl_PL, nl_NL, es_ES, pt_BR, fr_FR)

### Версионирование
- **PATCH** (0.0.X): исправление бага → Фаза 6
- **MINOR** (0.X.0): новая фича → Фазы 7–15
- **MAJOR** (X.0.0): несовместимые изменения протокола
- **VERSION протокола:** текущий **21** (IBRealistic.java), OBU base **18**. После реализации Фазы 17-18: → **23** (пакеты 80–88)
- **realistic_version:** текущая **1.1.0** (gradle.properties + pom.xml). После Фазы 17: → **1.2.0**
- **Каналы:** OBU — `openboatutils:settings` (пакеты 0–32), IBRealistic — `ibrealistic:settings` (пакеты 33–69, 80–88)

### Тестирование
Все изменения ДОЛЖНЫ тестироваться на:
- Minecraft 1.20.4
- Minecraft 1.21
- Minecraft 1.21.3
- С плагином TimingSystem (серверная часть)
- Без плагина (одиночная игра)
