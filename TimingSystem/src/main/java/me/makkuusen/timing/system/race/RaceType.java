package me.makkuusen.timing.system.race;

/**
 * Type of race session.
 */
public enum RaceType {
    /** Solo race — player isolated, no collisions, no visibility of others. */
    SOLO,
    /** Multiplayer race — multiple players race together with visibility and configurable collisions. */
    MULTIPLAYER
}
