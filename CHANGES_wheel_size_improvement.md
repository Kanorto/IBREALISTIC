# Изменения: Увеличение и улучшение колёс

## Дата
2026-02-11

## Краткое описание
Увеличены размеры колёс, улучшена их визуальная модель (октагональный профиль + обод), добавлен визуальный подъём лодки на колёсах, и колёса корректно выступают за ширину лодки.

## Изменённые файлы

### Мод (OpenBoatUtilsRealistic)
- `src/main/java/dev/o7moon/openboatutils/client/WheelRenderer.java` — полная переработка модели и размеров колёс
- `src/main/java/dev/o7moon/openboatutils/mixin/BoatEntityRendererMixin.java` — добавлен визуальный подъём лодки

## Детальное описание изменений

### 1. Увеличение размеров колёс
**Файл:** `WheelRenderer.java`
**Что сделано:**
- Радиус колеса: 0.25 → 0.4 блоков (диаметр 0.5 → 0.8 блоков, увеличение на 60%)
- Ширина шины: ~0.14 → ~0.24 блоков

**Причина:**
Прежние колёса были слишком маленькими ("микроколёсики") и выглядели непропорционально для раллийной машины.

### 2. Улучшение модели колеса
**Файл:** `WheelRenderer.java`
**Что сделано:**
- Вместо одного кубоида `wheel` — три части:
  - `tire_0` — основной кубоид шины (0° ориентация)
  - `tire_45` — второй кубоид шины (45° поворот по оси X) для октагонального профиля
  - `hub` — центральный диск обода (~56% от размера шины)
- Шина рендерится тёмной (0x1F1F1F / 0.12f RGB) с текстурой `black_concrete`
- Обод рендерится серым (0x595959 / 0.35f RGB) с текстурой `gray_concrete`

### 3. Колёса выступают за ширину лодки
**Файл:** `WheelRenderer.java`
**Что сделано:**
- Lateral offset: 0.55 → 0.70 (колёса выступают за борта лодки)
- Колёсная база тоже увеличена: ±0.55 → ±0.60

**Причина:**
Пользователь хотел чтобы колёса корректно выступали по ширине лодки.

### 4. Визуальный подъём лодки
**Файл:** `BoatEntityRendererMixin.java`
**Что сделано:**
- Добавлена константа `VISUAL_LIFT = 0.25f`
- В обоих рендерерах (≤1.21 и ≥1.21.3) лодка визуально поднимается на 0.25 блока
- Хитбокс НЕ изменяется — только визуальное положение

**Причина:**
Пользователь хотел чтобы лодка визуально стояла "на колёсах", выше чем обычно.

## Сравнение до/после

| Параметр | Было | Стало |
|----------|------|-------|
| Радиус колеса | 0.25 блоков | 0.40 блоков |
| Диаметр колеса | 0.50 блоков | 0.80 блоков |
| Ширина шины | ~0.14 блоков | ~0.24 блоков |
| Профиль | Квадратный (1 кубоид) | Октагональный (2 кубоида + обод) |
| Цвет шины | Тёмно-серый (0.15) | Очень тёмный (0.12) |
| Обод | Нет | Серый диск (0.35) |
| Y offset | 0.30 | 0.35 |
| Lateral offset | 0.55 | 0.70 |
| Axle offset | ±0.55 | ±0.60 |
| Визуальный подъём | Нет | 0.25 блока |

## Тестирование
- [x] Мод собирается успешно (Gradle) — все 3 версии MC
- [x] Протестировано на MC 1.20.4 (сборка)
- [x] Протестировано на MC 1.21 (сборка)
- [x] Протестировано на MC 1.21.3 (сборка)

## Примечания
- Изменения затрагивают только визуальный рендеринг — физика и хитбокс не изменены
- CODEBASE_INDEX.md нужно будет обновить при его создании
- Версия не изменена, т.к. это чисто визуальное улучшение существующей функциональности
